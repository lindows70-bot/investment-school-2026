# 📐 학생 화면 가독성 개선 체크리스트

## 1단계 — 토큰 램프 + 셸 + 브리핑
- [x] `theme.ts` FS 램프 상향 (micro 9.5→11 · tiny 11→12.5 · body 13→14 · lg 16→18 · xl 20→22 · h2 25→28 · h1 32→36)
- [x] 램프 변경 후 **weekly-report 육안 검증**(FS 최다 사용 화면 95곳 — 레이아웃 밀림 확인)
- [x] 셸: 브랜드 블록 264px → 로고 한 줄(약 72px) · DCF 공식·슬로건은 아카데미로 이관
- [x] 셸: 📌 매일만 펼치고 나머지 9그룹 접기
- [x] 셸: 모바일 하단탭에 브리핑 진입 추가 (도달 불가 해소)
- [x] 브리핑: 목차 헤더 → '오늘 한 줄' 결론 (기존 값 합산, 신규 계산 0)
- [x] 브리핑: 결론 글자 승격 ('버릴 것' 10→ · 플랜 결론 10.5→)
- [x] 검증: 브리핑 ≤10.5px **54%→15% 이하** · 크기 종수 **14→5 이하** · 사이드바 노출 메뉴 **9→14 이상** · 모바일 매일 5개 도달
- [x] `npm run check:build` → 배포 → 프로덕션 재측정

## 2단계 — 프리미티브 + 결론 승격
- [x] `Verdict`·`Section`·`Note` 신설 (신규 파일 → 커밋 훅이 토큰 강제)
      ↳ PageHead 는 Verdict 와 역할 중복, Row 는 화면마다 구조가 달라 보류(두 번째 소비자 생기면)
- [x] 자산: 총 평가금액 + 총 손익 헤드라인 (섹션 배지 합산 — '개수는 크기를 담지 못한다' 해소)
- [x] 대시보드: 내 총 수익률을 지수 22px보다 크게
- [x] 성적표: 배너 처방 → 데이터가 낸 판정으로 (배너·표 자기모순 해소)
- [x] 승패·주간: 결론 승격
- [x] 검증: 각 화면 첫 화면에 결론 블록 정확히 1개 · 카드 문법 한 벌 — **6화면 실측 통과**(2026-09-05).
      결론 블록: 브리핑·대시보드·주간·자산·승패·성적표 전부 1개(자산의 다른 28px 수치 2건은 top 1010·2401px 의 섹션 KPI 라 경합 아님).
      카드 문법: 5화면은 공용 `Section`·`Verdict`(TK.card #141824 · RAD.lg). **대시보드만 자체 문법**(bg0·bg7 등 5종) — 알려진 사실.

## 3단계 — 색·폰트·모바일
- [x] 모바일 폭 파손 수정 — globals.css 유틸 7개 + 5화면 (375px 가로 넘침 0)
- [x] 자산 화면 글자 크기 이관 (≤10.5px 53% → 11%, 남은 11%는 캔들차트 SVG 축 라벨)
- [x] 등락 색 규약 통일 — weekly pcol/pc 51곳 + jcol 분리, 같은 카드 자기모순 2건
- [x] Pretendard 실측 — OFL-1.1 · variable 통짜 2.0MB · dynamic-subset 92조각 3.1MB (도입 가능)
- [x] Pretendard 도입 + 인라인 fontFamily 59곳 → `FONT_STACK` (실전송 브리핑 643KB · 로그인 203KB · 레이아웃 회귀 0)
- [x] 위험색 재배치 — 93곳 전수 분류(A-등락 50 / B-위험 28 / C-계열색 9) 후 **충돌하는 14곳만** 이동.
      ⚠️ orange400 전용화는 **포기** — Satellite 계열색이 앱 전역 규약이라 비울 수 없다(사유는 context-notes).
- [x] 화면 실측 검증(로그인 상태) — KPI 줄·브리핑·주간에서 computed color 로 확인. 검증이 4건을 더 찾아냈다.
- [x] 검증이 찾은 것 ①: 대시보드 `WARN_CLR` 회귀 — 내가 orange400 으로 바꿔 **경고와 Satellite 가 한 색**이 됐다(SAT_CLR 가 두 줄 위). 되돌림.
- [x] 검증이 찾은 것 ②: 컴포넌트 레이어를 안 봤다 — 분류 워크플로가 page 파일만 훑어 `SwingStopAlertBanner`·`YieldCurveAlertBanner` 의 빨강 경보를 놓쳤다. 둘 다 이동(🔴 이모지도 ⚠️ 로 — 색만 바꾸면 한 줄 안에서 모순).
- [x] 검증이 찾은 것 ③: 브리핑 ⚔️ 승패 요약이 **미국식**이었다(오르는=초록). 같은 화면 위 DayMover 는 한국식이라 '오르는'이 두 색. `/win-lose` 처방 그대로 — 칩은 한국식, 요인 대비값은 **명도**로.
- [x] 검증이 찾은 것 ④: 대시보드 `coreLoss` 막대가 red400 인데 **그 막대의 툴팁**은 red400 을 'Core 수익'으로 썼다 → blue400.
- [x] `TenbaggerRadar` 등락 규약 (2026-09-06 · `0ed9dde`) — **한 채널에 한 뜻**으로 풀었다.
      색 = 부호(한국식)만. 단계(1루·2루·홈런)는 **막대 위치 + 마일스톤 눈금 + 이모지·문구**가 이미 말한다.
      '1배(원금)' 눈금은 blue400 → `sub3`(본전이 손실 파랑과 겹치면 안 된다). 10배만 금색 유지(축하 배지).
      ⚠️ 함수만 고쳤으면 화면은 그대로 빨강이었다 — 막대 채움·손잡이·카드 틴트·테두리가 `getBarColor` 를
      건너뛰고 `rgba(239,68,68)` 을 직접 그리고 있었다. **색 함수를 고칠 땐 그 색을 우회하는 자리를 함께 찾아라.**
      실측(프로덕션): 손실 `-9.0/-0.7/-47.5/-29.7/-25.2` 전부 파랑 · 수익 `+24.4/+24.5` 전부 빨강 · KPI 와 일치.
- [x] 브리핑 실적 요약 문자열 분리 — `earnResultsShared.ts` SSOT 신설, 판정(초록/빨강)과 주가(한국식) 따로.
      검증: 옛 join 로직과 108케이스 전수 대조 `diff=0` · 화면 실측(fetch 가로채 합성 2행) `상회 green400 + 주가 -2.5% blue400`.
- [x] 감사 2차(6화면 + 항상 렌더되는 컴포넌트 20개) — **저널에서 미검증 지적 44건 복구**해 직접 재현 확인 후 20곳 수정.
      ⚠️ 워크플로는 `survivors: 0` 을 반환했지만 70개 에이전트 중 **47개가 세션 한도로 실패**했다 — 반증이 아니라 미검증이다.
- [x] 남은 것 ①: `LynchInventorySentinel` 403·429 (2026-09-05 · `1588092`) — 조회 실패 배너 → `orange400`,
      수집 오류 카운트 → `amber400`(같은 뜻인 '데이터 제한'이 224·494행에선 amber 라 한 화면에서 두 색이었다).
- [x] 남은 것 ②: 대시보드 Core/Satellite 차트 — **한 채널에 한 뜻**으로 정리했다.
      색 = 진영(Core `sky400` · Satellite `orange400`, 보유표 배지·리밸런싱 위젯과 같은 짝) ·
      부호 = 0선 위/아래 + 명도(수익 0.95 / 손실 0.45). 범례·막대·툴팁 셋을 한 규칙으로 통일.
      **스택 막대**라 진영을 색으로 구분해야 하고 부호는 위치가 이미 말한다 — 그래서 색을 부호에서 뗐다.
      해소된 모순 3건: 범례 Satellite=sky400 ↔ 배지 Core=sky400 · Core 수익 막대 neonLime ↔ 툴팁 red400 ·
      Core 손실 막대 ↔ 툴팁이 서로 다른 색.
- [ ] 남은 것 ③: near-identical-hue 파편화 **13건**(assets 75·76·678·872 · weekly 32 · win-lose 125·179 ·
      EventCalendarPanel 38 · YieldCurve 53 · MarketCatalyst 32 · signal-report 381 · LynchInventorySentinel 487 · briefing 412).
      ※ EventCalendarPanel 85 는 실적 발표 식별색을 `violet400` 으로 옮기며 해소돼 목록에서 뺐다(14→13).
      **오독이 아니라 팔레트 위생**이라 ⛔ 일괄 치환 금지 원칙에 따라 손대지 않았다.
      ⚠️ 이 중 hex 하드코딩 2건(win-lose 179 `'#fdba74'` · weekly 32 `'#eda100'`)은 제1-b 위반이므로
         **그 화면을 다시 손볼 때 함께** 토큰으로 이관할 것(단독으로 열지 말 것).
- [~] 회색 18종 → 4단 수렴 — **하지 않기로 결정.** 1단계 실측에서 대비는 이미 WCAG AA 통과였고(sub 7.57·slate300 11.93)
      문제는 밝기가 아니라 **크기**였다. 세 번째 '회색 밝히기'는 회귀만 낳는다(⛔ 회색 또 밝히기 금지).
- [~] 액센트 차트 전용 격리(대시보드 글자색 43종) — **하지 않기로 결정.** 학생에게 보이는 이득이 없고
      건드릴 표면이 너무 넓다. 대신 '한 화면에서 한 색이 두 뜻'인 자리만 골라 고쳤다(이번 세션 20+6곳).
      ※ 그래서 옛 검증 기준 '대시보드 색 43→12 이하'도 폐기했다.
- [x] 검증: 한글 폰트 loaded ✅(`document.fonts.check('16px "Pretendard Variable"')` → true) ·
      **375px 가로 스크롤 0 — 6화면 전부 통과**(브리핑·대시보드·주간·자산·승패·성적표, overflowPx=0).
      ⚠️ '대시보드 색 43→12 이하'는 액센트 격리를 하지 않기로 해서 **적용되지 않는 기준**이다(폐기).
- [x] 검증이 찾은 위반: **설명 문장이 각주 크기(11px)** — 사용자 상설 규칙 위반.
      `FirmHandsCard` 판정 근거·처방 2곳(자산 화면 5문장) · 주간 리포트 설명 3곳 → `FS.tiny`.
      색은 그대로 뒀다(⛔ 회색 또 밝히기 금지 — 대비는 이미 AA 통과, 문제는 크기였다).
      실측: 자산 5→0 · 주간 16→13(남은 13건은 출처·기준일·캐비엇이라 FS.micro 의 정당한 용도).
- [x] 남은 것 ④: 대시보드 탭 부제 10px → `FS.micro`(라벨도 12→`FS.tiny`).
      ⚠️ **"조밀한 격자라 밀린다"는 내 판단이 틀렸다** — 220px 세로 목록이라 세로로만 늘어난다.
      프로덕션 브라우저에서 값을 바꿔 재 보니 총높이 2833→2953px(+4.2%) · 가로 넘침 0(데스크톱·375px 양쪽).
      ⭐ 짐작으로 두 번 미뤘던 건이다. **레이아웃 걱정은 10초 실측으로 갈린다.**
- [x] 색 겸직 3건 — `LynchInventorySentinel` 403(조회 실패)·429(오류 카운트) · `EventCalendarPanel` 실적 발표 식별색 → `violet400`
- [x] 갱신 시각 노출 2건 — `src/lib/freshness.ts` SSOT 신설. `CocktailPartyGauge`(asOf 가 타입에만 있었다) ·
      `MarketBreadthPanel`(표본·기준·데이터일·갱신을 카드 상단으로). ⚠️ 기준은 FedWatch 의 렌더 시각이 아니라 각 API 의 `asOf`.

## 4단계 — 모바일 실측 (2026-09-11 · 로그인 상태 375px · 6화면 + 모달 + 서랍)
- [x] 종목 추가 모달 저장 버튼이 하단 탭바에 덮임(z 100 = 100) → z 1000 (`e013287`). **휴대폰에서 종목 추가가 불가능했던 차단 결함.**
- [x] 기록 서브탭 5개 61×174px 세로 깨짐 · 9열 표 눌림 → nowrap + `.m-scroll` · 표 minWidth 720 + `.m-scroll-hint` 캡션
- [x] 대시보드 글로벌 지수 3열 → 22px 숫자 6/6 잘림(`26,081.`) → `.m-2col`
- [x] 자산 29,694px(36.5화면) → 차트·수급 섹션을 모바일에서 토글 뒤로(렌더 자체를 안 함) → 15,220px(18.7화면). 남은 절반은 카드 헤더 418 + 포트폴리오 표 481 — 접힌 한 줄 목록은 별도 설계 건.
- [x] iOS: `100dvh` + `safe-area-inset-bottom` + `viewportFit: 'cover'` (`b09cfa3`). ⚠️ 에뮬레이터로 못 잰다 — 실제 iPhone 확인 필요.
- [x] 404 한글 안내(`not-found.tsx`)
- [ ] 작은 글자(<11px) 비율 — 승패 48% · 대시보드 47% · 성적표 47% · 자산 22%(차트 축) · 주간 3% · 브리핑 0%.
      승패 179행·성적표 381행·대시보드 탭은 **Ruflo 시범 과제(N2·N3·N6)와 겹쳐** 시범 뒤 파일 단위로 이관.
- [ ] 브리핑 '오늘 한 줄' 결론이 dev 첫 로드에서 37초 뒤 채워짐 — 전 섹션을 기다리는 구조. 프로덕션(캐시)에서 재측정 후 판단.
- [~] 44px 터치 규칙으로 칩 버튼이 커지는 것(자산 배지 16 · 승패 기간 3 · 대시보드 4) — 화면에서 보니 깨지지 않고 누르기 편해 **그대로 둠**.

## 상시
- [ ] 결정할 때마다 `context-notes.md`에 append
- [ ] ⛔ 일괄 치환 금지 · ⛔ 정보 삭제 금지 · ⛔ 회색 또 밝히기 금지
