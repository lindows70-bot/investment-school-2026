# 📐 학생 화면 가독성 개선 체크리스트

## 1단계 — 토큰 램프 + 셸 + 브리핑
- [x] `theme.ts` FS 램프 상향 (micro 9.5→11 · tiny 11→12.5 · body 13→14 · lg 16→18 · xl 20→22 · h2 25→28 · h1 32→36)
- [x] 램프 변경 후 **weekly-report 육안 검증**(FS 최다 사용 화면 95곳 — 레이아웃 밀림 확인)
- [x] 셸: 브랜드 블록 264px → 로고 한 줄(약 72px) · DCF 공식·슬로건은 아카데미로 이관
- [x] 셸: 📌 매일만 펼치고 나머지 9그룹 접기
- [x] 셸: 모바일 하단탭에 브리핑 진입 추가 (도달 불가 해소)
- [x] 브리핑: 목차 헤더 → '오늘 한 줄' 결론 (기존 값 합산, 신규 계산 0)
- [x] 브리핑: 결론 글자 승격 ('버릴 것' 10→ · 플랜 결론 10.5→)
- [x] 검증: 브리핑 ≤10.5px **54%→15% 이하** · 크기 종수 **14→5 이하** · 사이드바 노출 메뉴 **9→14 이상** · 모바일 매일 5개 도달
- [x] `npm run check:build` → 배포 → 프로덕션 재측정

## 2단계 — 프리미티브 + 결론 승격
- [x] `Verdict`·`Section`·`Note` 신설 (신규 파일 → 커밋 훅이 토큰 강제)
      ↳ PageHead 는 Verdict 와 역할 중복, Row 는 화면마다 구조가 달라 보류(두 번째 소비자 생기면)
- [x] 자산: 총 평가금액 + 총 손익 헤드라인 (섹션 배지 합산 — '개수는 크기를 담지 못한다' 해소)
- [x] 대시보드: 내 총 수익률을 지수 22px보다 크게
- [x] 성적표: 배너 처방 → 데이터가 낸 판정으로 (배너·표 자기모순 해소)
- [x] 승패·주간: 결론 승격
- [ ] 검증: 각 화면 첫 화면에 결론 블록 정확히 1개 · 카드 문법 한 벌

## 3단계 — 색·폰트·모바일
- [x] 모바일 폭 파손 수정 — globals.css 유틸 7개 + 5화면 (375px 가로 넘침 0)
- [x] 자산 화면 글자 크기 이관 (≤10.5px 53% → 11%, 남은 11%는 캔들차트 SVG 축 라벨)
- [x] 등락 색 규약 통일 — weekly pcol/pc 51곳 + jcol 분리, 같은 카드 자기모순 2건
- [x] Pretendard 실측 — OFL-1.1 · variable 통짜 2.0MB · dynamic-subset 92조각 3.1MB (도입 가능)
- [x] Pretendard 도입 + 인라인 fontFamily 59곳 → `FONT_STACK` (실전송 브리핑 643KB · 로그인 203KB · 레이아웃 회귀 0)
- [x] 위험색 재배치 — 93곳 전수 분류(A-등락 50 / B-위험 28 / C-계열색 9) 후 **충돌하는 14곳만** 이동.
      ⚠️ orange400 전용화는 **포기** — Satellite 계열색이 앱 전역 규약이라 비울 수 없다(사유는 context-notes).
- [x] 화면 실측 검증(로그인 상태) — KPI 줄·브리핑·주간에서 computed color 로 확인. 검증이 4건을 더 찾아냈다.
- [x] 검증이 찾은 것 ①: 대시보드 `WARN_CLR` 회귀 — 내가 orange400 으로 바꿔 **경고와 Satellite 가 한 색**이 됐다(SAT_CLR 가 두 줄 위). 되돌림.
- [x] 검증이 찾은 것 ②: 컴포넌트 레이어를 안 봤다 — 분류 워크플로가 page 파일만 훑어 `SwingStopAlertBanner`·`YieldCurveAlertBanner` 의 빨강 경보를 놓쳤다. 둘 다 이동(🔴 이모지도 ⚠️ 로 — 색만 바꾸면 한 줄 안에서 모순).
- [x] 검증이 찾은 것 ③: 브리핑 ⚔️ 승패 요약이 **미국식**이었다(오르는=초록). 같은 화면 위 DayMover 는 한국식이라 '오르는'이 두 색. `/win-lose` 처방 그대로 — 칩은 한국식, 요인 대비값은 **명도**로.
- [x] 검증이 찾은 것 ④: 대시보드 `coreLoss` 막대가 red400 인데 **그 막대의 툴팁**은 red400 을 'Core 수익'으로 썼다 → blue400.
- [ ] `TenbaggerRadar` 등락 규약 — **미국식**(수익 emerald / 손실 red)이라 같은 대시보드에서 `-47.5%` 가 KPI 는 파랑, 여기선 빨강이다.
      ⚠️ 절반만 고치면 안 된다 — 이 컴포넌트는 '성과 램프'(1루 파랑 → 2루 초록 → 홈런 주황 → 10배 금색)와 등락색이 엉켜 있어,
      손실만 파랑으로 바꾸면 램프의 1루(blue400)와 부딪힌다. 램프를 먼저 정하고 한 번에 옮길 것.
- [ ] 브리핑 실적 요약 문자열 분리 (판정 / 주가반응 — 색으로는 못 고친다 · `earnResults.ts:138`)
- [ ] 회색 18종 → 4단 수렴 (slate100 / slate300 / sub / slate500)
- [ ] 액센트 차트 전용 격리 (대시보드 글자색 43종)
- [ ] 검증: 대시보드 색 **43→12 이하** · 한글 폰트 loaded · 375px 가로 스크롤 0

## 상시
- [ ] 결정할 때마다 `context-notes.md`에 append
- [ ] ⛔ 일괄 치환 금지 · ⛔ 정보 삭제 금지 · ⛔ 회색 또 밝히기 금지
