# 🚪 출구 플랜 — 설계 (2026-07-31)

## 목적
매수엔 플랜 카드(1%룰·분할·손절선)가 있는데 매도 계획은 8종 신호가 화면마다 흩어짐 — 보유 종목마다 "언제·얼마나 팔지"를 한 카드로. 학생 최대 약점(매도 결정)을 계획으로 전환.

## 데이터 (전부 기존 SSOT — 신규 수집 0)
- 캔들 = getTechCandles 일별 캐시(타이밍 크론이 매일 전 보유 종목 워밍)
- 타점·신호 = timingFromCandles(trendBreak·bearDiv·과대이격·급락)
- 🛎️ 이익 보호선(신규 계산) = **샹들리에 스탑 표준: 22봉 최고가(high) − 3×ATR(22)** — techSignals 순수 수학
- 섹터 국면 = rotationShared 캐시 읽기 · 펀더 판정 = user_daily_briefings 최신일(Jarvis 크론 적재분)

## 계산 (결정론)
- 두 참고선: 🛡️ 최후 방어선(구름 하단) + 🛎️ 이익 보호선(샹들리에). 행동 한 줄은 우선순위 결정론(구조 붕괴 > 보호선 이탈 > 수익+소진 > 미발동).
- 샹들리에를 "매수 후 최고가"가 아닌 **표준 22봉**으로: 학생마다 값이 달라지면 제2원칙 위반 + 워처(종목 단위) 알림과 불일치.

## 정직 캐비엇
매도 지시 아님(참고선) · 저점 매도 강요 금지(손실+펀더 멀쩡=변동성 구분) · 자동매매 없음 · 224봉 미만 신생은 정직 생략.

## 구현
lib/exitPlan.ts(SSOT) · /api/exit-plan(auth·fp 캐시 6h) · ExitPlanBoard(자산 관리 마운트) · timing-watch 크론 protBreak 알림 추가.
