# 🎯 스윙 타점 체크리스트

## 0단계 — 측정 (이게 실패하면 기능은 만들지 않는다)
- [ ] 백테스트 하네스 복구 + **커밋**(`scripts/backtest-swing.mjs` — 실제 techSignals 컴파일, 재구현 금지)
- [ ] 14종 셋업 × **5봉·10봉·15봉** 전방수익 재측정(baseline 대비 초과분만)
- [ ] **'밥그릇 3번' 셋업 추가 측정**(224 돌파 → 거래량 수축 눌림 + 공구리) — 기존 14종과 같은 잣대
- [ ] **이평선 길이 비교**: 200(우리 관례) vs 224(단테) vs 245(실제 1년) — 224가 임의인지 낫는지 나란히 잰다
- [ ] **목표 도달률 측정** — "익절 +15~30%"가 몇 %나 실제로 닿는가(손익비만으론 아무 주장도 아니다)
- [ ] `backtest-autopsy` 4단(종목 분산·최다 점유율·시점 분산·이상치 제거)
- [ ] **레짐 분리** — 상승/중립/하락 국면별 성적(정예 타점이 "상승 전용"이었던 전례)
- [ ] 결과 보고 → **살아남은 셋업이 없으면 "스윙 우위 없음"으로 종료**(억지로 만들지 않는다)

## 1단계 — SSOT
- [ ] `src/lib/swingSetup.ts` — 진입·목표가·손절·기간손절·R:R 판정(순수 함수)
- [ ] 손절은 `exitPlan`/샹들리에 **재사용**(새 계산 금지 — 제2원칙)
- [ ] 국면 게이트 — `momCrash`·`countryVol` 재사용

## 2단계 — API·화면
- [ ] `/api/swing-radar` (`swing-radar-v1:{date}` · 부분실패 박제 금지 · 크론 워밍)
- [ ] `/swing` 화면 + 사이드바
- [ ] 카드마다 **edge·승률·표본수** 병기 · 국면 경고 상시 노출(끌 수 없음)
- [ ] ⛔ 목표 수익률 약속 금지 · 세금·수수료·슬리피지 미반영 명시

## 3단계 — 검증·기록
- [ ] 독립 재계산(원천 캔들에서 목표가·손절 재계산해 화면값과 대조)
- [ ] 화면검증(브라우저 실접속 — 오늘 라운드에서 10건이 여기서만 잡혔다)
- [ ] tsc → check:build (`&&` 체이닝) → 배포
- [ ] 전향적 성적 적립 연결(축별 성적과 같은 관례 — 표본·시점 분산 게이트 포함)
- [ ] CLAUDE.md + `docs/README.md` 기록

## 4단계 — 운영 중 발견 (2026-09-11 검토)
- [x] 🕯️ 장중 미완성 봉 판정 결함 — 완성 봉만 · 진입일=신호 봉 · 크론 06:15 KST · v13 (context-notes 마지막 절)
- [x] 카드의 백테스트 값이 "이 기법 성적"으로 표기 — "백테스트(5년)" 명시 + 실제 적립 승률 병기 (`1c4499e`)
- [x] 08-14 추격 가드 이전 표본·09-11 이전 장중가 기록 — 행마다 '옛 규칙'·'장중가' 표시 + 범례 (`1c4499e`)
- [x] 설명 문단 16곳 `FS.micro` → `FS.tiny` (`9ec6aa1`)
- [x] 투자금 자동 채움(`/api/cash-position` totalKrw) + localStorage 기억 + 출처 표시 (`9ec6aa1`)
- [x] `tracks.on` 을 상한 절단 **전** 목록으로 판정 · 잘린 트랙은 "상한에 걸려 미표시" 문구 (`9ec6aa1`)
- [x] cron-health 가 결과 캐시 대신 **크론 실행 마커**(`swing-cron-run-v1:{date}`, refresh=1 경로만 기록)를 봄 · v14 (`9ec6aa1`)
      ⚠️ 배포 당일(9/13)은 전날 마커가 없어 stale 로 보인다 — 9/13 06:15 크론부터 정상.
- [x] 해외 상장(거래소 접미사) 스캔 제외 + `skippedForeign` 화면 표시 — 664종 중 113종 (`9ec6aa1`). 미국 트랙은 미국 40종 검증.
- [ ] 다음 스윙 신호가 나오면 진입가 = 네이버/야후 **종가** 인지 대조(완성 봉 수정의 마지막 확인 — 9/11~13 신호 0건이라 아직 못 봄)
- [x] ⚠️ **같은 결함(스윙 밖)** — `entryTiming`(11개 소비처 입구)·`hi52Radar`·`tech-screener`·`marketBreadth` 에 `dropIncompleteBar` 적용, 키 v3/v4/v2 (`0445ca3`). 실측: 시장 폭 US 기준일 09-11(진행 봉) → 09-10(완성 봉).
      `signal-report` 는 '지금 수익률' 표시라 현재가가 맞아 제외.
