# 📚 개발 기록 2026-08 — 실적 리포트(SEC/DART)·화면검증 라운드·성적표 재설계

> CLAUDE.md에서 분리한 완료 기능 기록입니다(2026-08-03 분할). 원칙·반복 함정은 CLAUDE.md에 남아 있습니다.
> 여기 기록은 **왜 그렇게 만들었나·무엇을 기각했나**를 찾을 때 봅니다.

## 🔍 매수/매도 4종 합동 검증 라운드 + 빌드 사고 (2026-08-01)

사용자 검증샷(재진입 감시·출구 플랜·매매 브리핑) 전수 대조 — **수치 전부 독립 재계산 일치**:
- **👀 재진입 감시 8종**: 낙폭 7종 소수점 일치·제외 조건 전부 정당(속도/크기), NXPI 격차 −10.6%p = NXPI −11.2 − SPY −0.6 정확. ⭐ **삼성E&A 자동 복귀 실증** — 7/29 급락 제외(−25.7%)였던 종목이 낙폭 −12.9%·고점 18봉 전으로 게이트가 스스로 풀려 담을 것 1위(83) 복귀. QCOM은 낙폭이 −24.7%로 깊어져 RS 사유→급락 사유로 전환(둘 다 정당).
- **🚪 출구 플랜**: US 4종 금요일 새 봉 보호선·방어선 소수점 일치, KR 3종 주말 불변 정합. 담을 것 5종 vs 감시 8종 겹침 0.
- **원익IPS 🟡→🟢 하루 만에 바뀐 것은 버그 아님** — money-flow 실측 CROWDED→INFLOW(금요일 세션에서 외국인 전환). 산 이유 점검은 설계대로 매일 갱신되는 현재값을 반영.
- 🩹 검증 발견 1건: 한 카드에 '🟢 산 이유 유지 — 변동성일 가능성' vs '→ 펀더 경고·정리 우선순위 상위' 공존(원익IPS) — thesis 4축은 보존인데 Jarvis SELL은 **별도 룰**(칼날·FCF·분기 마진 추세) 발동. intact+SELL이면 "기록된 산 이유(4축)는 유지 — 단 펀더 진단은 별도 룰로 경고 중"으로 축 병기(ETN 융합 전례 5번째).
- 🛎️ 보호선 이탈 알림은 첫날 베이스라인(+주말)이라 미발동이 정상 — 월요일 크론부터.

⚠️ **빌드 사고 — 무인자 GET 라우트의 빌드 시 정적 생성(잠복 결함 발현)**: 배포 중 Vercel 빌드가 `/api/macro-data` 정적 생성 3회 타임아웃으로 실패(SEC·Yahoo 지연 — 변경 코드와 무관). **nps-portfolio 사건과 동일 클래스**인데 5월 초기 라우트들엔 force-dynamic이 누락돼 있었고, 지금까지는 빌드 때 60초 안에 우연히 끝나서 지나갔던 것. 전수 스캔 → **9곳 일괄 선언**(macro-data·shadow-13f·cocktail-party·exchange-rate·ghost-stock·macro-weather·market-indices·v1 2종). 부수 교정: 정적 생성 라우트는 **빌드 시점 데이터가 박제**되는 정합성 문제도 함께 소멸. ⭐ 교훈: **"지금까지 빌드가 됐다" ≠ "라우트 설정이 맞다"** — 무인자 GET + 외부 fetch 라우트는 전수 force-dynamic이 기본값이어야 하고, 외부 API가 느려지는 날 갑자기 빌드가 죽는 형태로 발현된다.

## 👻 유령 추적기 화면검증 — KR 리포트 5건 캡 결함 발견·SSOT 수정 (2026-08-01)

실데이터 교체 후 첫 화면검증. Ghost Score 9종목 산식 검산 전부 일치(SK하이닉스 72=35+30+7·NVDA 21=4+10+7·SPCX 42=12+10+20), 내부자(DART 실공시: SK하이닉스 3건 ₩4.7억·KB금융 3건 ₩6.4억)·US 커버리지(Yahoo 58·55·25·27명)·기관보유율 전부 원본 일치. 배지·"매도 미추적" 표기·ETF 13종 가드·출처 각주 정상.
- 🚨 **검증이 잡은 결함 — KR 커버리지가 전 종목 정확히 '5건'**: 네이버 `integration.researches`는 **최근 5건 고정 상한**(실측: 삼성전자도 5) — 커버리지 척도로 쓰면 전 KR 종목이 '소외'로 오판. **SK하이닉스(실제 90일 33건 = 최다 커버리지)가 '특급 유령'으로 뜬 진범**. 점수 산식은 맞는데 입력이 캡된 값이었다.
- **수정(SSOT — getAnalystSignal)**: 페이징 리서치 API `m.stock.naver.com/api/research/stock/{code}?pageSize=100`로 **최근 90일 실제 건수** 집계(실측: 삼성전자 49·SK하이닉스 33·KB금융 12·인텔리안테크 7·원익IPS 2). 실패 시 5-cap 폴백·0건도 실데이터로 존중.
- ⭐ **부수 효과 — 이 캡은 텐배거·정성진단의 KR 언더커버리지(≤3건) 판정도 조용히 망가뜨리고 있었다**: 원익IPS는 실제 2건인데 5로 보여 '숨겨진 진주' 자격을 놓치던 상태. 라이브 검증(공개 텐배거 라우트): 원익IPS 2건→PASS·SK하이닉스 33건→FAIL — 세 기능(유령·텐배거·getQualitativeChecks) 동시 교정.
- ⭐ 교훈: **API가 주는 배열의 length를 '개수'로 쓰기 전에 상한 유무를 실측하라** — 전 종목이 같은 값(5)이면 데이터가 아니라 캡이다(점수 산식 검산이 다 맞아도 입력이 캡이면 판정은 틀린다).
- 부수 발견(같은 날): 타점 워처 예약 크론 8/1 아침 1회 미발화(코드 정상 — 수동 실행 복구·protBreak 64종 적재·이탈 40종이 출구 플랜과 교차 일치). 월요일 발화 모니터링.

## 🔍 유령 추적기 '미보유 발굴' 섹션 (2026-08-01)

사용자 질문("예전엔 미보유 중소형 위주 아니었나?")에 git 고고학으로 답 — **첫 구현(5/27)부터 보유 종목 연동**이었고 v1 목업도 당시 보유주 6종(로직 변경 없음·기억은 10배거 헌터/위성 발굴과 혼동). 단 지적의 본질이 맞다: 린치 유령 철학은 발굴인데 보유 점검만 하면 반쪽 → 실데이터 교체로 발굴이 가능해진 시점에 섹션 추가.
- `buildDiscovery`: 위성 풀(SAT_SCORE_KEY·중소형 100종) 칼날 제외 상위 24종 → **buildGhostRecord SSOT 그대로** 스캔 → Ghost Score 상위 12. 전 학생 공유 일일 캐시(`ghost-discovery-v1:{date}`) — 유니버스 기반이라 개인 데이터 없음, 서빙 시 각자 보유분 제외. 보유 분석과 병렬 발사.
- 캐비엇 명시: 매수 신호 아님(조사 후보 — 종합 매수 판정·재무 직접 확인) · 후보 풀은 큐레이션 100종(전 시장 전수 아님). 실측: 위성 풀 97종 중 칼날 제외 37종(하락장) — 상위 에이피알·Zeta·Beam·First Solar 등.

## 👻 유령 추적기 검증 2라운드 — 목적 명료화 + 버딕트 사실 정합 (2026-08-01)

수정 후 재검증: **21종(보유 9+발굴 12) Ghost Score 산식 전수 검산 일치** · 에이피알 리포트 14건 원본 일치 · 발굴 내부자 0건 = EDGAR/DART 실조회 적재분 확인 · SPCX 기관 2% = Yahoo 1.5% 실데이터(신규상장 13F 반영 전) · 재배치 정확(SK하이닉스 33건→핫플·원익IPS 2건→진주·특급 유령 0).
- 사용자 질문("목적이 뭐야? 헛갈린다")이 정곡 — **도구 축 정의**: 유령 추적기 = WHAT(6축)·WHEN(신호등)과 다른 제3의 축 **'WHO KNOWS(정보 우위)'**. Ghost 점수는 품질이 아니라 '시장이 아직 모르는 정도' — 낮음(총공세)=나쁜 회사가 아니라 다 알려진 회사.
- 🩹 헛갈림의 원인이 도구 자신에게: ① **진주 버딕트 거짓+매수 권유** — 내부자 0건인데 "내부자 매수 신호가 잡힙니다·**선점하세요**"(5월 목업 문구가 등급 조건과 불일치한 채 잔존) → net 사실 기반 분기 + 매수 권유 제거(조사 권유까지만) ② '강한 매수' 배지 vs "소규모 매수" 톤 충돌 → net≥2 고확신 승격 ③ KR 단위 '명'→'건' ④ '발굴 초기' 밴드 ≤10 조정 ⑤ 헤더에 점수 의미·축 구분 설명 추가. 발굴 공유 캐시는 옛 문구 박제라 퍼지(내일부턴 자연 갱신).
- ⭐ 교훈: **등급 문구는 등급 '조건'의 전 조합에서 참이어야 한다** — pearl은 내부자 0건도 성립하는데 문구가 "내부자 매수가 잡힌다"고 단정하면 절반의 케이스에서 거짓말이 된다(같은 null 다른 폴백 교훈의 문구 버전).

## 👻 유령 추적기 v3 — 발견 사이클 지도 재설계 (2026-08-02)

사용자 피드백("의미 모호·복잡·매수 추천처럼 보임·제외 자산 지저분")으로 패널 전면 재설계(860→290줄·데이터 계약 불변).
- **점수 링·게이지 카드 → 🗺️ 발견 사이클 지도**: 유령→진주→주목→핫플→총공세 5단 컬럼에 종목 칩 배치 — 이 화면의 질문("내 종목이 발견 사이클 어디인가")에 맞는 시각. 축 설명 병기("왼쪽=시장이 모름·조사 가치 / 오른쪽=다 앎·선점 이점 없음"). 🔥=90일 임원 장내매수만 칩에 노출.
- **Ghost 점수 격하**: 카드 전면 큰 링(72점!) → 상세 카드 안 '관측 점수(품질 아님)' 작은 표기 — "점수 = 추천 강도"로 읽히던 오독의 원흉 제거. 상세(커버리지·내부자·린치 한마디)는 칩 클릭 시에만.
- **제거**: 린치 분석 제외 자산 카드 13개(푸터 한 줄 개수로만) · 4개 카운트 카드 · No.1 배지 · 필터/정렬 · 파이프라인 상태바 · 긴 원칙 인용 박스. 헤더에 "매수 추천 아님 — 품질은 6축·타이밍은 신호등" 축 구분 한 줄.
- ⭐ 교훈: **화면의 시각 언어가 질문과 어긋나면 아무리 정확해도 오독된다** — 점수 링+게이지는 "추천 강도"로 읽힌다. 관측 렌즈는 '지도(위치)'로 그려야 한다.

## 🔥 내부자 매수 레이더 — 유령 추적기 주인공 교체 (2026-08-02, v4)

v3 발견 사이클 지도도 하루 만에 기각(사용자: "이 화면으로 뭘 하라는 건지 모르겠다 — 명확·유용하지 않으면 없애자"). **솔직한 판단: 이 화면의 행동 가능 정보는 '임원이 자기 돈으로 산 종목' 하나뿐** — 발견 사이클 개념은 지적으로 맞아도 할 일을 주지 못한다. 없애는 대신 주인공 교체(포트폴리오+발굴 풀의 내부자 실매수 집계 = 타 화면에 없는 고유 가치).
- 메뉴명 👻 유령 종목 추적기 → **🔥 내부자 매수 레이더** · ① 보유 중 임원 매수 = 헤드라인 카드(금액·건수·N일 전·👥2명 이상 고확신 + 커버리지 맥락을 학생 언어로) ② 발굴 = 소외+내부자 조합(0건이면 "하락장엔 임원들도 지갑을 닫는다 — 그 자체가 정보" 정직 공백) ③ 매수 없는 종목 = '할 일 없음' 명시·축소 칩. Ghost 점수 UI 전면 제거. API·데이터 계약 불변.
- ⭐ 교훈: **화면의 존재 이유는 '학생이 다음에 할 일'이 나오는가로 판정한다** — 개념적으로 옳은 시각화(발견 지도)도 행동이 안 나오면 실패. 행동 가능한 신호 하나(내부자 실매수)를 헤드라인으로 올리고 나머지를 전부 배경으로 내리는 게 답이었다(두 번의 재설계 끝에 도달).

## 🔥 내부자 매수 — 추천·브리핑 3곳 연결 (2026-08-02)

사용자 질문("레이더 내용이 실제 추천·매매 브리핑에도 반영되나?")에 정직 감사 — **부분만 반영 중**이었다(Jarvis BUY 룰 30일 매집·10배거 헌터·US 수급 프록시엔 있으나, KR 수급 축은 외인/기관/개인만이라 **최태원 62.2억(7/31 공시·레이더가 하루 만에 포착 실증)이 레이더 밖에선 안 보임**). 3곳 연결(전부 배지·알림 — ⛔ 점수 미반영, Jarvis BUY 룰에 이미 있는 축이라 이중계상 방지):
- ① **타점 워처 크론**: SigState.insAmt(90일 장내매수 총액) 증가 감지 → '🔥 임원 장내매수 공시' 매수측 알림(아침 브리핑 ①·배너). 개별 주식만·undefined-safe 첫날 오탐 0·insider_signals 24h 공유 캐시라 부하 미미.
- ② **종합 매수 판정 v14**: 🔥 pro "임원 장내매수 N건 — 회사 내부자가 자기 돈을 걸었다(+2명 이상 고확신)". 라이브 검증: SK하이닉스 4건·고확신 표출 ✓.
- ③ **통합추천 최종 12종(UNIFIED_RECO_V v56)**: 🔥 배지 — 리밸런싱·퀀트빌더·본부장 브리핑 자동 상속.
- SSOT = getInsiderSignal 단일 소스(레이더·판정·배지·알림이 같은 값). ⚠️ 크론 알림은 다음 크론부터 베이스라인 적재 후 신규 공시에 발동.

## 🚨 크론 헬스 모니터 (2026-08-01) — 조용한 미발화 감지 + 자동 복구

8/1 타점 워처 크론 미발화 사고(코드 정상·조용히 안 돎)의 재발 방지 — 크론 13개의 산출물(캐시 updated_at·DB 스냅샷)을 매일 09:40 KST에 자동 점검. 설계 docs/cron-health/.
- **`lib/cronHealth.ts`(SSOT)**: 크론 13개 ↔ 산출물 매핑(키는 전부 lib 상수 import — WIN_LOSE_KEY·MARKET_FLOW_KR_KEY·SAT_SCORE_KEY·UNIVERSE_KEY·FUND_CACHE_KEY) + `lastExpectedKst`(KST·요일 반영 — ⚠️ UTC 월요일 크론=KST 화요일 발화) + 판정(ok/pending[유예 45분]/stale).
- **`/api/cron-health`**: 크론 호출(CRON_SECRET 일치)일 때만 stale의 경량 idempotent 크론 자동 재실행(240s 예산·morning-briefing은 heavy로 마지막), 일반 GET은 보고만. 브리핑 상단에 미발화 빨간 배너(없으면 렌더 0).
- ⚠️ **검증이 잡은 설계 결함 2건(라이브 대조)**: ① **워밍형 크론(nps·honeycomb·shadow13f)은 예정 시각 판정이 오탐** — 라우트가 캐시 TTL로 재계산을 스스로 결정해, 사용자가 중간에 캐시를 갱신하면 크론이 정상 실행돼도 updated_at 불변 → `ttlH` 모드(최대 허용 나이=TTL+24h+유예)로 분리 ② **죽은 키를 감시할 뻔** — 'shadow-13f-funds'는 7/25 CUSIP 작업 때 v4로 바뀐 옛 키(옛 키가 7/25에 멈춘 건 당연한데 "7일째 미발화"로 오경보) → guru13f `FUND_CACHE_KEY` export해 SSOT import.
- 검증: 단위 13케이스(요일·주간·유예 경계) + 라이브 13/13 ok(8/1 크론 전부 정상 실측) + 프로덕션 API 대조 일치.
- ⭐ 교훈: **헬스 모니터 자체가 캐시 키 사고의 소비자가 된다** — 감시 키를 리터럴로 박으면 키 버전업 순간 영구 오경보. 감시자야말로 SSOT import가 필수. + 워밍형(TTL 판단)과 스케줄형(항상 재계산) 크론은 "실행됐나"의 증거가 다르다.

## 📰 어닝 결과 모닝 브리핑 (2026-08-01) — 발표 다음날 beat/miss + 주가 반응

보유 종목 실적 발표 다음날 아침, 매매 브리핑에서 결과를 바로 확인(어닝 시즌 시의성 — PLTR 8/3·NVDA 8/26). 설계 docs/earnings-results/.
- **Phase 0 실측이 설계를 확정**: Yahoo `earningsHistory`는 발표 직후 actual+**surprisePercent** 반영(GOOGL 7/22 발표분 +214%·SK하이닉스 .KS도 +85% — KR 제공 실측). 단 **지나간 발표일은 어느 모듈도 안 줌**(발표 후 calendarEvents가 다음 분기로 점프) → **미래 발표일을 공유 맵(`earn-dates-v1`)에 사전 적립**하고 날짜가 지나면 그 날을 앵커로 결과 계산.
- **`lib/earnResults.ts`(SSOT)**: 적립(라우트 자급 — 캘린더 방문 의존 없음·과거 21일 유지) + beat/miss(**surprisePercent 제공값 사용** — 재계산 금지·제2원칙) + **결합 검증**(발표일−분기말 <100일 — 미발표 종목의 이전 분기 결과를 이번 발표로 오인 방지, MPC 122일 실측) + 반응(발표일 **이전** 마지막 종가→최신 종가 — BMO/AMC 불명이라 전일 앵커 통일). AI 미사용·결정론 문장 조립.
- **`/api/earnings-results`**(auth·fp 캐시 6h) + 브리핑 ①¾ 📰 섹션(발표 없으면 렌더 0·Jarvis 어닝콜 딥링크).
- 검증(실컴파일 4케이스): GOOGL beat +214.2%·반응 +5.8% / **SK하이닉스 +85.2%·반응 +30.0% = 7/31 실제 상한가**(1,322,000→1,718,000 검산 — 이상값이 아니라 실데이터) / MPC '집계 중' / 창(3일) 밖 제외 — 캔들 원천 재계산 소수점 일치.
- ⚠️ 첫날 한계(정직): 적립 맵이 오늘부터 쌓여 첫 실전 표시는 PLTR(8/3 발표) 다음날부터.

## 📊 시장 폭(Breadth) 레이더 (2026-08-01) — 막스 시계추 탭 + 브리핑 ⑤ 칩

"지수는 오르는데 내 종목은 다 빠진다"의 정체(소수 대형주 장세)를 수치로 — 박경철 강의 때 "전 종목 데이터 제약"으로 보류했던 신고저 breadth를 **기술 검색기 캔들 인프라 편승(신규 수집 0)**으로 실현. 설계 docs/market-breadth/.
- **`lib/marketBreadth.ts`(SSOT)**: 유니버스 647종(UNIVERSE_KEY) × getTechCandles 일별 캐시 → US/KR 시장별 **250일 breadth 시계열**(200/50일선 위 비율·52주 신고/신저[종가 기준]·상승 비율) + 자기 역사 백분위 + 밴드(>75 과열/45~75 건강/25~45 약화/<25 침체) + **다이버전스**(지수 60일 고점권 −3% 이내인데 폭이 60일 전 대비 −5%p↓=상투형 / 반대=바닥형). 집계 캘린더 = 지수(^GSPC·^KS11) 봉. 전체 계산 9초(캐시 히트).
- **`/api/market-breadth`**(공개·`BREADTH_KEY` 일별 캐시·refresh·유효 60% 미만 박제 금지·최근 3일 폴백) + 크론 09:35(검색기 캔들 워밍 직후) + **cronHealth 모니터 즉시 등록**(신규 크론은 헬스 등록이 한 세트).
- ⛔ 점수·선정 미반영(관측 전용·WHAT/WHEN 분리) · 캐비엇: 유니버스 기준(전 시장 전수 아님)·종가 기준 신고저·백분위 표본 ~250일.
- **검증**: 독립 재구현(직접 평균) 대조 **완전 일치** — US 60.4%(250/414)·KR 27.1%(48/177), 스팟체크 3종 SMA200 판정 정합, 프로덕션 라이브 = 로컬 동일. ⭐ 첫 판정이 장세를 정확히 반영: **KR 27.1% = 자기 역사 백분위 1.6%(사실상 1년 최저)·60일 전 76.4%→붕괴** vs US 60.4% 건강 — 한국 폭락장의 속살이 첫날부터 수치로 드러남.
- 교훈: 무거운 전 종목 지표는 **캔들 워밍 크론 뒤에 편승**하면 계산 비용이 캐시 히트 수준으로 떨어진다(검색기 09:10 → 신고가 09:25 → breadth 09:35 파이프라인). + route 파일 export 금지 재확인(BREADTH_KEY는 lib에).

## 💰 현금 등록 (2026-08-01) — 실제 현금 비중 vs 막스 권장 밴드

막스 시계추가 "권장 현금 20~30%"라 말해도 **내 실제 현금이 몇 %인지 몰라 비교 불가**였고 주간 리포트 현금 게이지는 'unknown'이었다. 현금은 앱이 알아낼 원천이 없는 유일한 자산 → **학생 직접 입력(Zero-Input 원칙의 명시적 예외)**. 설계 docs/cash-position/.
- **`user_cash` 테이블**(사용자당 1행·krw/usd/memo·RLS 본인만) — ⚠️ **Supabase SQL Editor에서 `supabase/user_cash.sql` 1회 실행 필요**(미실행 시 `needsSetup` 안내만 표시·앱 무중단, re_watchlist 관례).
- **`lib/cashPosition.ts`(SSOT)**: ⭐ **`cashBandOf` — 밴드 산식이 MarksCycle·브리핑에 인라인으로 흩어져 있던 것을 통합**(새 숫자 발명 0) + `evaluateAssets`(stock-price 40청크·**currency 기준 환산**[크립토 KRW ×1380 폭증 방지]·**라이브 실패 시 원가 폴백**[0% 붕괴 교훈]) + 밴드 대비 판정(경계값은 밴드 안).
- **`/api/cash-position`**(GET/PUT·개인 캐시[현금값 지문 포함]·평가액 0이면 박제 금지) + **CashPositionCard**(자산 관리 상단 — 입력·비중 바에 권장 밴드 구간 오버레이).
- **연결 3곳**: 막스 시계추 권장 밴드 옆 "내 현금 N% · 밴드 안/적음/많음" 병기(미등록 시 등록 링크) · 브리핑 ⑤ 칩 · **주간 리포트 현금 게이지 unknown → 실측 전환**(온도 fetch가 없어 범용 임계 5/10% 사용·사이클 연동 판정은 막스로 안내 = 축 차이 명시).
- ⛔ "현금 늘려라/줄여라" 지시 아님(밴드 대비 위치 관측) · 자동 연동 없음·갱신일 표기 · 부동산·연금 등 앱 밖 자산 미포함(캐비엇).
- 검증: 밴드·판정 경계 **9케이스 전수 통과**(온도 74/75 분기·19.9/20.0/30.0/30.1% 경계) + 실DB 평가액 재계산 정합(교사 22행 원가 ₩31,322,716) + 프로덕션 401 게이트 확인.
- ⚠️ **investments 행으로 현금을 저장하지 않은 이유**: 35개 화면이 investments를 읽으므로 현금 행이 섞이면 수익률·평가액·분류가 통째로 오염된다(퀀트빌더 가상종목 사건과 동일 클래스) — 별도 테이블이 정답.

### 💱 환율 하드코딩 전면 제거 (2026-08-01, 현금 카드 화면검증이 발견)
현금 카드 첫 화면 검증에서 **같은 화면 위아래가 서로 다른 환율을 쓰고 있었다** — 상단 KPI '총 투자금액 ₩3,103만'(하드코딩 1,350)과 현금 카드 '투자 자산 ₩2,663만'(라이브 1,438). 실제 환율 1,445 대비 **7% 과소 표기**. 제1원칙(데이터 하드코딩 금지)·제2원칙(같은 지표 같은 값) 동시 위반이 5월 초기 코드부터 잠복해 있었다.
- **전 저장소 스캔 → 9곳 수정**: 서버 4(morningstar-rating·portfolio-flow·portfolio-reco-kr·school-league — **비중·₩권장액·학생 총자산이 전부 1,350 고정**) + 클라 5(assets 총투자금액·평가액·배당 / **dashboard 총자산 KPI** — 라이브 usdKrw state가 있는데 모듈 함수 `toKrw`만 상수 사용 / analysis / admin / SchoolLeague).
- **`lib/fx.ts` 신설**: `getUsdKrw(base)`·`fetchUsdKrw`(라이브 + 실패 시에만 폴백) — 서버 라우트 공용 SSOT. 남은 상수는 전부 `*_FALLBACK` 명명 + 폴백 전용 주석(의도가 코드에 드러나게).
- ⛔ 유지: `TradePlanCard`의 1380은 **라이브 도착 전 로딩 폴백**(유럽 통화는 아예 대기) — 정당한 폴백이라 그대로.
- ⭐ 교훈 1: **하드코딩 상수는 "지금은 맞는 값"으로 시작해 조용히 거짓말이 된다** — 1,350은 도입 시점엔 근사했지만 지금 7% 오차. 커밋 훅이 색상·fontSize는 잡지만 **환율 같은 도메인 상수는 못 잡는다**(값만 봐서는 하드코딩인지 알 수 없음).
- ⭐ 교훈 2: **새 기능(현금 카드)이 기존 기능의 잠복 결함을 드러냈다** — 같은 화면에 정확한 수치를 하나 더 놓자 기존 수치가 틀렸다는 게 즉시 보였다. 화면검증이 매번 값어치를 하는 이유.

## 📉 커버드콜 X-Ray (2026-08-01) — 배당 인컴 랩 하단

유튜브(박차장 「연 15% 평생 받는다」) 검토 요청에서 출발. 영상의 알맹이는 상품 추천이 아니라 **판단 기준**이었다 — "분배율"이 아니라 **본주 대비 총수익(TR) 갭**으로 커버드콜을 봐야 한다. 앱엔 이미 커버드콜 유니버스·함정 플래그·"분배율이 높다고 총수익은 아닙니다" 경고가 있었지만 **전부 정성 문구뿐**이라 실제 크기를 알 수 없었다. 설계 docs/covered-call-xray/.
- **Phase 0 실측**: Yahoo `adjclose`(배당 재투자 TR) − `close`(가격) = **분배 기여분 분리**. KR ETF도 `.KS`로 동일 제공. 벤치마크는 종목명 전수 조회로 검증(482730='TIGER US S&P500 +10% Premium'→SPY 등).
- **`lib/coveredCall.ts`(SSOT)**: 15쌍 매핑 + **공통 거래일 정렬**(상장일 차이 보정) + 1년·2년·전체 3창 + 갭 판정(−2/−10%p) + **원금 침식**(가격 하락) 별도 경고. `/api/covered-call-xray`(공개·일별 캐시·성공 60% 미만 박제 금지) → `/dividend` 하단. 유니버스에 482730·458760 편입.
- ⛔ **의도적 제외**: 펀드of펀드(YMAG·YMAX·ULTY — 단일 본주 없음) · 영상의 특정 상품 추천(실측이 반박) · ISA·연금 이전 절세(세무 상담 영역).
- ⚠️ **라이브 검증이 잡은 자기모순 2건**: ① **proxy 매핑은 결론을 뒤집는다** — 498410을 KODEX 은행에 붙이면 −53.2%p, KODEX 200이면 −195.5%p. 벤치 선택이 판정을 좌우하면 그 비교는 정보가 아니라 소음이라 제외(plan에 "억지 근사는 거짓 비교"라 써놓고 proxy를 남긴 모순) ② **갭이 작다고 양호가 아니다** — MSTY는 본주도 폭락해 갭 −0.6%p뿐인데 총수익 −42.8%·가격 −91.6%에 🟢'추종 양호'가 붙었다 → 총수익 마이너스면 `both_down`(🔻 본주와 동반 하락)으로 분리. **"배지가 숫자를 상쇄하지 못한다"는 이 앱의 반복 교훈이 또 나왔다**.
- **검증**: 단위 8케이스(판정 경계·공통일 정렬·표본 미달) + 라이브 독립 재계산 4쌍 완전 일치 + 프로덕션 15/15.
- 🚨 **화면검증 2라운드 — 환율 오염(중요)**: 국내 상장 ETF(**원화**)를 US 본주(**달러**)와 그대로 비교해 **환율 상승분(2년 +2.7~7.3%)이 갭에 얹혀** 있었다. 의심의 출발점은 부호였다 — **커버드콜은 상방을 팔았으니 구조상 본주를 크게 이길 수 없는데** 화면엔 RISE +8.5%p·TIGER S&P500 +4.3%p가 🟢'추종 양호'로 떴다. 통화 정규화(`needsFxAlign` — 본주를 같은 날 환율로 원화 환산, 환율 결측일도 공통 구간에서 제외) 후: **RISE +8.5→−4.5%p(🟢→🟡) · TIGER S&P500 +4.3→−3.3(🟢→🟡) · TIGER 나스닥100 +2.9→−10.2(🟢→🔴) · KODEX 데일리 −11.5→−19.0**. ⭐ 결과가 뒤집혔다 — **국내 커버드콜 4종 중 본주를 따라간 건 TIGER 미국배당(−0.7%p) 하나뿐**이고, 영상이 추천한 나스닥·S&P500형도 전부 뒤처졌다.
  - ⭐ 교훈: **비율 지표(PEG·마진)는 환율 무관이지만 수익률은 통화에 종속된다** — 섹터 피어 X-Ray의 "비율이라 환율 무관" 교훈을 수익률 비교에 그대로 적용하면 안 된다(정반대 케이스). 그리고 **부호가 상식과 어긋나면 데이터를 의심할 것**.
- ⭐ **실측이 드러낸 교육 핵심**: 같은 나스닥100인데 **타겟형(486290) −10.2%p vs 데일리 전량형(494300) −19.0%p** — 이름이 비슷해도 옵션 매도 구조가 결과를 가른다(영상도 안 짚은 부분). ⚠️ 영상 주장 중 "S&P500·다우존스 커버드콜은 본주와 큰 차이 없다"는 **환율 보정 전엔 맞아 보였지만 보정 후엔 −3.3%p·−0.7%p로 약해진다**(다우존스형만 근접).

## 💵 배당 인컴 랩 성장률 낙관 편향 수정 (2026-08-01, 커버드콜 X-Ray 화면검증 후속)

X-Ray를 붙이자마자 바로 위 프로젝션과의 모순이 드러났다. 초고배당형 화면이 **'배당 성장률 +1.0%'**라 표시했는데 실제 데이터는 커버드콜들이 분배를 **급격히 축소 중**(494300 −27.4%·CONY −76.8%·YMAG −52.1%). 원인은 `yocProjRate ?? 0` — 5년 CAGR이 null인 종목(**비중 75%**)을 전부 '성장 0%'로 채워 감소가 통계에서 사라졌다. 실측 가중평균 **−7.3%**(커버리지 80%). 이 +1.0%가 프로젝션(30년 월 1,387만·자산 7.52억)과 목표 역산(8년)의 입력이었다.
- **수정**: 폴백 체인 `yocProjRate → dividendGrowth1y → 둘 다 없으면 가중에서 제외`(0으로 메우지 않는다) · 성장률 음수면 빨강·'분배 축소 중'·실측 커버리지 % 병기 · 프로젝션 '불어난다' 문구 분기 · 목표 역산 '—' → **'도달 불가' + 이유**('분배 축소가 재투자 효과보다 커서 이 조합으론 늘지 않습니다').
- **검증(고배당 인컴형)**: 연 483만·월 40만·안전성 69점·성장률 7.3%(실측 100%)·프로젝션 12.1% 복리·목표 7.46억/18년 — 전부 재계산 일치. 초고배당형 10종 산수도 완전 일치(연 배당 ₩22,132,600).
- ⭐ 교훈: **null을 0으로 채우면 '모른다'가 '변화 없다'로 둔갑한다** — 특히 감소하는 데이터에서 이 폴백은 낙관 편향을 만든다. 그리고 **정확한 수치를 한 화면에 하나 더 놓으면 기존 수치의 오류가 즉시 드러난다**(같은 날 현금 카드가 자산관리 환율 오류를 드러낸 것과 동일 패턴, 2번째).
- ⚠️ 남은 정직 한계: 연 배당의 **48.6%가 추정 분배율**(CONY 60%·PLTW 55%·YMAG 45% = 운용사 목표치, 실제는 매월 크게 변동 — 화면에 경고 있음).

## 💱 환율 기여도 분해 (2026-08-01) — 자산 관리

"달러로는 벌었는데 계좌는 왜 마이너스지?"를 숫자로 답한다. **같은 날 환율 때문에 두 번 데인 뒤**(자산관리 KPI 하드코딩 1,350 · 커버드콜 X-Ray 원화/달러 혼용) 앱을 뒤져보니 환율을 *표시*하는 곳은 10곳인데 **수익을 환율로 분해하는 화면은 0건**이었다 — 개발자가 놓친 축을 학생도 못 보고 있었다. 설계 docs/fx-attribution/.
- **Phase 0 실측**: `investments.purchase_date`가 USD 보유 **26행 전부 존재(100%)** + Yahoo `KRW=X` 일봉 → **매입일 환율 조회 가능**(주간 리포트가 "매입환율 미보유로 현재 환율 근사"라 적어둔 한계가 실은 해결 가능했다).
- **`lib/fxAttribution.ts`(SSOT)**: 행 단위로 `원화 수익 = 달러 수익 + 환율 기여` 분해(매입일 환율은 그날 종가·휴장일은 직전 거래일) + **분할매수는 원가 가중 평균 매입환율** + **부호 뒤집힘 감지**(달러 이익인데 원화 손실). `/api/fx-attribution`(auth·fp 일별 캐시) + `FxAttributionCard`(자산 관리·USD 없으면 렌더 0) + 환율 ±5·10% 시나리오.
- **검증 15/15**: 단위 12(fxAt 경계 4·환율만 +20% → 달러 0%/원화 +20%·뒤집힘·분할매수 가중이면 기여 정확히 0%p) + 라이브 3(별도 구현 재계산과 완전 일치).
- ⭐ **실측이 드러낸 것**: 교사 포트 해외분 **달러 −7.1% vs 원화 −9.2%(환율 −2.1%p)**, 7월 초 1,520~1,529원 매수분은 −5~6%p. **PHO는 달러 +1.8%인데 원화 −4.4%**, XLP +0.3% → −5.2% — 종목은 올랐는데 계좌가 마이너스인 이유가 환율이라는 걸 3종이 실증.
- ⛔ 예측·환헤지 권유 아님(과거 분해) · **원화 상장 해외 ETF(TIGER 미국S&P500 등)는 대상 제외**(원화로 사고팔아 매입 환율 개념이 없음 — 억지로 넣으면 근거 없는 숫자, 커버드콜 proxy 기각과 동일 논리).
- ⚠️ `for..of Map` **TS2802가 4번째 재현** — `Array.from(map.entries())`. 문서에 3회 기록돼 있어도 또 걸린다.

## 📑 실적 리포트 (2026-08-01) — SEC 8-K 원문 자동 수집·요약 · 종목 확인 그룹

사용자가 NotebookLM 링크를 주며 "이런 형태로 2026년 최신 실적보고서를 볼 수 있나" 물었다. 검토 결과 **NotebookLM의 본질은 원문 IR 문서를 AI가 읽는 것**이고, 그 원문을 우리가 무료·자동으로 가져올 수 있었다. 사용자 확정 규모 50종. 설계 `docs/earnings-report/`.
- **Phase 0 실측이 설계를 확정**: SEC `company_tickers.json`(10,412종)→CIK → `submissions`에서 **8-K 중 `items`에 '2.02'(Results of Operations)** 필터 → full submission `{acc}.txt`에서 **문서 `<TYPE>`으로 EX-99.x 추출**. 4/4 성공(NVDA는 EX-99.1 보도자료 20KB + **EX-99.2 CFO 코멘터리** 15KB까지).
- ⚠️ **파일명 정규식은 2/3 실패** — NVDA `q1fy27pr.htm`·GOOGL `googexhibit991q22026.htm`(회사마다 제각각). **첨부 선택은 파일명이 아니라 문서 TYPE으로**.
- ⛔ **컨콜 Q&A 전문은 정직하게 제외**(SEC 공시 대상 아님 — Motley Fool·Seeking Alpha는 유료·스크랩 리스크). 실적 보도자료+CFO 코멘터리로 실적·가이던스·세그먼트는 커버. **한국은 v1 제외**(DART 잠정실적이 숫자 위주라 '요약할 서술'이 없음).
- **구성**: `lib/earningsReport.ts`(SSOT — CIK 맵·8-K 탐색·EX-99 추출·HTML 정제·Gemini 요약) + `/api/earnings-reports`(서빙) + `/api/cron/earnings-reports`(배치·09:20 KST·`cronHealth` 등록) + `/earnings-reports` 화면(목록·종목 심층·기업 비교). 요약은 **서술만** — 판정·점수는 상세 패널에서 `ResearchVerdictCard`(6축·타점) 재사용.
- ⭐ **대상 50종은 동적**: 하드코딩 티커 리스트 대신 유니버스 시총 상위. `ScreenedStock.marketCap` 노출(fcfYield 계산에 이미 쓰던 값 — **추가 fetch 0**), `UNIVERSE_KEY` v10→v11.
- 🚨 **첫 실측에서 대상 상위 50이 전부 일본 종목**(미쓰비시UFJ 1위) — 원인 둘: ① `ScreenedStock.market 'US'`는 실은 **'한국이 아님'**이라 일본(.T)·홍콩(.HK)이 섞임 ② `marketCap`이 **원시 통화**라 환산 없이 정렬하면 40조 엔이 $40,270B로 읽혀 달러 종목을 압도. → SEC 8-K는 미국 상장사만 제출하므로 **`currency==='USD'` + 접미사 없음**으로 좁히면 두 문제가 함께 해소. 대상도 '상위 50 고정'이 아니라 **시총 순으로 훑다 수집 성공 50개를 채우면 중단**(미국 예탁증서는 6-K 제출이라 자동 스킵되고 다음 순위가 그 자리를 채움 — 실측 57종 훑어 50 수집·실패 7).
- **검증**: 실제 lib을 tsc 컴파일해 실행(재구현 금지) → 수집 5/5 · **SEC 원문 직접 재조회 독립 대조**(TSLA 5/5·AAPL 2/2·MSFT 1/1 수치 실재 = 환각 0) · 50종 요약 전수 품질 스캔.
- ⚠️ **요약 품질은 프롬프트만으로 안 잡힌다(3연속 실측)**: ① CVX가 **영어로** 출력(한국어 지시가 약함) → 절대 규칙으로 승격 ② tone이 10건 전부 positive라 **변별력 0** — 실적 보도자료는 원래 낙관적으로 쓰이므로 절대 기준으로는 신호가 안 됨 → "같은 종류의 문서들 사이에서 **상대적으로**" 판정하도록 기준 명시(수정 후 45/4/1로 분화, TSLA가 매출 +26%인데 영업이익 −57%·FCF 마이너스라 cautious로 정확히 갈림) ③ XOM에 중국어 수 단위 `万` 혼입 → 프롬프트 금지 + **후처리 치환**(万·亿·兆 — 한국어 실적 문장에 정상적으로 올 일이 없어 안전). 높임체 13종도 평서형으로 통일.
- ⭐ 교훈: **LLM 출력 규약(언어·문체·표기)은 프롬프트로 지시하고 후처리로 확정한다** — 프롬프트만 믿으면 50건 중 몇 건이 조용히 샌다. 그리고 **어조·등급 같은 판정 필드는 '절대 기준'이면 전부 같은 값이 나와 무의미** — 같은 종류 문서 안에서의 상대 판정이어야 신호가 된다.
- 운영: 프롬프트 수정 후 특정 종목 재요약은 `?force=TICKER,TICKER&limit=N`.
- 🚨 **화면검증이 잡은 수치 오류(2026-08-02) — 문체 지시가 숫자를 망가뜨렸다**: 한자(`万`)를 막으려고 넣은 **"수 단위는 한글로만 쓴다(만·억·조)"**를 Gemini가 **"숫자를 한글로 표기하라"**로 읽어, 재요약한 14종에서 숫자가 한글로 풀려 쓰였다. 결과가 문체 문제가 아니라 **수치 오류**였다 — **JNJ "매출 이십오조 삼백십억 달러"**(원문 $25.3 Billion = **1,000배**) · **TXN "주당순이익 2원 14센트"**(원문 $2.14 = **통화가 원화로 뒤바뀜**) · PLTR "팔십오 퍼센트". → 프롬프트를 "숫자는 반드시 아라비아 숫자"·"금액은 원문 통화 그대로"로 명시하고 단위 낱말만 한글로 제한.
- ⭐ **재발 구조를 없앤 것이 본질**: `summaryIssues()`(한자·높임체·영어·**한글숫자**·**통화오류** 기계 검사)를 크론이 매 실행 적용해 **걸린 종목을 자동 재요약**한다(응답 `stillFlagged`로 잔여 노출). 프롬프트를 고칠 때마다 사람이 50종을 훑지 않아도 스스로 낫는다 — 실측: 배포 후 1회 실행에 5종 자동 교정·잔여 0.
- ⭐ 교훈: **LLM에 준 문체·표기 지시는 수치까지 바꾼다** — "단위는 한글로"가 "숫자를 한글로"가 됐다. 서술 지시와 수치 규약은 **문장을 분리해 각각 못박고**, 검사 가능한 형태(정규식)로 기계 검증을 붙여야 한다. 프롬프트만 고치고 끝내면 다음 수정 때 또 샌다.
- 🇰🇷 **한국 실적 카드 신설(2026-08-02) — 서술 대신 숫자**: 사용자가 제미나이의 "한국도 완전히 가능" 보고서를 가져와 검토 요청 → DART 원문 전수 대조 결과 **데이터 확보 주장은 맞고 나머지는 과장**이었다(컨콜 Q&A 전문·부문별 실적·증권사 리포트는 DART 밖). 🚨 **더 결정적으로 제미나이 리포트 자체에 오류 2건**: ① 삼성전자 절대값은 정정 후(171.50·89.49)를 쓰고 **증감율은 정정 전**(+129.3%·+1,810.3% / 실제 +130.00%·+1,813.83%) — 정정 표가 한 공시 안에 나란히 있어 원문을 읽었다면 불가능한 조합 ② **현대차 영업이익 −20.8%**(3.60조→2.85조)를 안 쓰고 "분기 최대 매출"만 강조. → **원문에서 뽑으면 안 생기는 오류**라 이 기능의 존재 이유가 실증됐다.
  - **`lib/krEarnings.ts`(SSOT)**: 잠정실적 공시를 **`<TR>/<TD>` 셀 단위로 파싱**(평문화하면 빈 칸이 사라져 슬롯이 밀린다). 실측 함정 셋 — ① **단위가 회사마다 다름**(삼성전자 `조원` / SK하이닉스 `백만원`) → 표 머리 라벨을 읽어 원 단위 정규화 ② **정정공시가 따로 옴** → 접수번호 최대 건 채택 ③ **회사마다 채우는 항목이 다름**(삼성전자는 순이익 칸이 빔) → 빈 칸은 정직하게 null.
  - **⚠️ 매출↑ 이익↓ 자동 감지**(`divergence`) — 헤드라인에 묻히는 대비를 목록에서 바로 드러낸다. 첫 수집 실측 8곳(현대차·기아·현대로템·현대글로비스·한국항공우주·LG에너지솔루션·POSCO홀딩스·NAVER) = 관세·환율 국면이 자동차·중공업에 집중된 그림.
  - ⚠️ **흑자전환이 '—'로 사라지던 것**(수집 후 발견): 전년이 적자면 증감율(%)이 성립하지 않아 공시가 `흑자전환` 문구를 대신 주는데 목록이 %만 읽어 버렸다 → 같은 자리에 문구 표시(흑자=초록·적자=빨강), divergence도 '적자 전환' 포함. 실측 3종(삼성SDI·SK이노베이션·S-Oil).
  - `/api/kr-earnings` + `/api/cron/kr-earnings`(09:15 KST·cronHealth 등록) + 실적 리포트 화면 🇺🇸/🇰🇷 탭. 대상 = 유니버스 KR 시총 상위 50(동적). 검증: 실제 lib을 tsc 컴파일해 실행 — **3사 12개 값 전부 DART 원문과 일치**. 첫 수집 50종 23초.
  - ⚠️ TS2802(`for..of`/스프레드 on matchAll) **5번째 재현** → `Array.from`.
  - 🚨 **화면검증(2026-08-02) — 기간 라벨이 대부분 빈칸이었다**: 목록에 **2026년 2분기 36곳과 1분기 14곳이 섞여 있는데**(아직 2분기 미발표) 분기 표기가 없으면 학생이 전부 최신으로 오해한다. 원인 = 표기 형식이 **11가지**(`(26.2Q)`·`('26.2Q)`·`(2026년 2분기)`·`(26년 2분기 )`·`(2026.04.01~2026.06.30)`·`(2026.4.1~2026.6.30)`·`(1Q'26)`·`('26.Q1 )`·괄호 없는 `2026년 1분기`·괄호 없는 `26.1.1~ 26.3.31`·`2026년 1/4분기`)인데 정규식이 둘만 알고 있었다. 여섯 가지로 늘려도 4종이 남아 **접근을 바꿨다 — 표기를 쫓지 말고 표 구조를 읽는다**: 헤더 행(증감율 셀이 둘인 행)에서 증감율·흑자적자 칸을 뺀 나머지가 `[당기·전기·전년동기]`. 형식과 무관하게 3개를 얻고 정규화 실패 시 원문 그대로(빈칸보다 낫다) → **50/50 확보**. 다수와 다른 분기는 앰버로 강조(14곳).
  - ⭐ 교훈: **자유 서식 공시는 텍스트 패턴이 아니라 표 구조로 읽어야 한다** — 값(셀 단위 파싱)에서 배운 것을 헤더에도 그대로 적용해야 했다. 정규식을 늘리는 방향은 11번째 표기가 나올 때까지 끝나지 않는다.
  - ⚠️ **수치는 원문 대조 전부 일치**(신한지주 3.99조·2663억·1875억 / 하나금융 11.72조·2037억·+783.27% / 우리금융 −25.03% / KB금융 31.76조 / 한화에어로 9.29조). 내가 "금융지주 순이익이 너무 작다"고 의심했던 신한지주 1,875억도 **공시 원문 그대로**였다 — 상식과 어긋나 보여도 공시가 진실이면 그대로 옮긴다.
  - ⚖️ **비교가 비교가 아니었다(사용자 화면검증)**: 미국 비교는 요약 카드를 옆에 늘어놓은 것이라 **AAPL은 총마진·GOOGL은 영업이익·NVDA는 데이터센터 매출**을 말해 같은 항목이 같은 줄에 없었다. NVDA는 73일 전 **다른 회계분기** 자료인데 분기 표시조차 없어 같은 시점으로 읽혔다. → ① 요약 스키마에 **정량 필드**(period·revenue·revenueChange·eps·epsChange) 추가해 표로 줄을 맞추고 서술(어조·헤드라인·가이던스)은 표 아래로 ② **실적 분기·발표일을 첫 두 줄**에 둬 회계연도 차이를 먼저 보게 ③ `summaryIssues`에 '정량필드' 검사를 넣어 **크론이 스스로 재요약**(50/50 완비까지 3배치). ④ **한국 비교 신설** — 매출·영업이익·순이익이 이미 같은 축의 숫자라 미국보다 깔끔하고, **영업이익률(영업이익÷매출)은 앱이 계산**해 넣는다(규모가 다른 회사를 견주는 유일한 공통 잣대). 실측: 삼성전자 매출 171.5조·이익률 52.2% vs SK하이닉스 79.32조·**76.3%** — 규모는 삼성이 2배인데 수익성은 SK가 앞선다는 대비가 한 줄로 드러난다.
  - 🚨 **비교 검증(2026-08-02) — EPS만 있으면 일회성 이익이 실력처럼 보인다**: 표에 매출·EPS만 두니 **GOOGL EPS +294%**가 압도적으로 읽혔다. 원문 확인 결과 **영업이익 $31,271M→$40,770M(+30%)** 인데 **기타수익(순)이 $2,662M→$97,983M**(지분증권 평가이익)이라 순이익·EPS만 폭증한 것 — 본업은 MSFT(+18%)·AAPL(+16%)과 같은 급이다. → **영업이익 행 추가**(opIncome·opIncomeChange) + 표로 바꾸며 뺐던 **핵심 실적을 서술 카드에 복원**. 이제 표에서 "왜 EPS만 튀나"가 보이고 카드가 *"주식 시장의 지분 평가이익 반영에 힘입어"*로 답한다. 부수 발견: 매출은 GOOGL이 33% 큰데 **영업이익은 407.7억 vs 406.0억으로 거의 같다**(MSFT 이익률이 훨씬 높다).
  - ⚠️ **MSFT 매출 "900억 7만 달러"**(원문 $90,007M = 900억 **700만**) — JNJ 1,000배 사고와 같은 뿌리. **금액 자릿수 변환을 LLM 판단에 맡기면 반복해서 샌다** → 프롬프트에 원문 백만 달러 표기 변환 예시 3개를 못박음. (원문 대조 자동 검산은 미구현 — 남은 과제)
  - ⚠️ **자가 치유가 무한 루프가 될 뻔했다**: 영업이익 누락을 재요약 트리거에 넣었더니 **WFC(은행)·MRK(제약)는 원문에 영업이익 표기가 아예 없어** 매일 헛되이 Gemini를 호출하게 됐다 → 트리거에서 제외하고 정직하게 빈칸. **자동 재시도 조건은 "다시 하면 채워질 수 있는 것"에만** 걸어야 한다.
  - ✅ 수치 검증: 미국 16개(NVDA·AAPL·GOOGL·MSFT 각 4) + 한국 20개 **전부 원문 일치** · 영업이익률 4종(52.2/76.3/5.8/12.7%) 검산 일치 · NVDA `+214%`·GOOGL `EPS increased 294%`는 원문 문장 그대로.
  - ⭐ 교훈: **여러 대상을 '나란히 놓는 것'은 비교가 아니다** — 자유 서술을 병렬 배치하면 각자 다른 지표를 말해 눈이 대조할 축이 없다. 비교하려면 **정해진 칸(공통 축)이 먼저** 있어야 하고, LLM 요약을 비교에 쓰려면 스키마에 그 칸을 박아야 한다.
  - 🧭 **펼치기 표시 추가(사용자 지적)**: 카드를 눌러야 상세가 열리는데 표시가 없어 학생이 존재를 모른다 → 미국·한국 카드 모두에 `자세히 ▾ / 접기 ▴` 배지(미국은 비교 모드에선 숨김 — 그때 클릭은 선택이라 뜻이 다르다).
- 🇰🇷 **서술 요약은 여전히 불가(실측 확정)**: 잠정실적 공시 원문은 **숫자 표 + 정형 면책 문구**뿐이고, 분기보고서의 경영진단(MD&A)은 *"기업공시서식 작성기준에 따라 **분기보고서에는 본 항목을 기재하지 아니하였습니다**"*로 **제도적으로 비어 있다**(연 1회 사업보고서에만). 서술형 실적 설명은 회사 홈페이지 IR 자료라 자동화 불가. **미국은 SEC가 실적 보도자료 전문을 표준 공시(8-K Item 2.02)로 받는데 한국은 같은 층위 문서를 규제기관이 받지 않는다** — 앱 한계가 아니라 공시 제도 차이. 판정표 `docs/earnings-report/context-notes.md`.
  - ⭐ **이래서 미국 카드에만 '자신감/중립/신중' 어조 배지가 있다** — 어조는 **회사가 쓴 문장의 톤**을 상대 판정한 값인데, 한국 잠정실적엔 판정할 문장 자체가 없다. 대신 한국엔 미국에 없는 **영업이익률(앱이 매출÷영업이익으로 직접 계산)**과 **⚠️ 매출↑ 이익↓·흑자/적자 전환** 표식이 있다 — 서술이 없으니 숫자로 대비를 드러내는 쪽으로 설계가 갈렸다. 같은 기능의 두 시장 카드가 다른 축을 보여주는 건 데이터 원천의 차이지 한쪽이 덜 만들어진 게 아니다.
- 🔢 **금액 검산 3종 확정(2026-08-02)** — 프롬프트로 못 막는 자릿수 사고를 기계로 잡는다. ① **만 단위 자릿수**: 백만 달러 공시를 억·만으로 옮기면 만 단위는 100의 배수(3자리 이상)다. `212억 3만`(만 단위 3)·`900억 7만`(7)처럼 1~2자리면 끝자리를 흘린 것. ⚠️ '백만 단위 정수성'으로 판정하면 **천 달러 단위까지 공시하는 회사**(KLA 36억 5,755만=$3,657.55M·넷플릭스 125억 5,993만)를 오탐한다 — 실측으로 걸렀다. ② **원문 실재**(백만·십억 표기 + 자릿수 경계) — 연간/분기 혼용·기타수익 포함 합계를 잡는다. ③ **표기 정규화**: `212억 3백만`·`39억 4천9백만`은 **값은 맞는데** 다른 카드(`1,700만`)와 표기가 어긋나고 파서가 못 읽어 오탐한다 → 재요약이 아니라 `ko()` 결정론 치환으로 편다(万/亿/兆와 같은 자리). 12/12·8/8 케이스 통과.
- 🚨 **XOM 매출이 비었던 진짜 원인 = 입력 절단(프롬프트 아님)**: 매출 행이 원문 **34,347자** 위치인데 `MAX_INPUT` 30,000자에 잘려 **모델에 도달한 적이 없었다**. 프롬프트에 실제 예시("Sales and other operating revenue 114,529")까지 넣고 재요약해도 계속 비었다 — **데이터를 안 주고 지시만 한 셈**. → `clipForPrompt`: 상한을 넘으면 앞 24,000자(보도자료 서술)에 **손익계산서 구간 6,000자를 앵커로 찾아 덧붙인다**(총량 불변). 수정 후 `1,145억 2,900만 달러 +44.1%` = 원문 114,529/79,477 검산 일치.
  - ⭐ 교훈: **LLM이 지시를 계속 무시하면 프롬프트를 세 번째로 고치기 전에 "그 데이터가 입력에 들어가 있나"를 먼저 확인하라.** 잘린 입력은 에러도 경고도 없이 조용히 빈칸을 만든다(Supabase 1,000행 상한과 같은 절단 클래스 — 이번엔 문자 수).
- ✅ **정량 칸 50/50 완비·플래그 0**(2026-08-02): 라운드 검증에서 잡아 고친 것 — PG 분기 선택(연간 $87,032M→분기 $21,203M·EPS 실제 −15%) · CVX 매출(기타수익 포함 700억 5,500만→순수 671억 9,900만 +51%) · XOM 절단 · BAC 영업이익(은행은 원문에 없어 정직하게 빈칸 — WFC·MRK와 동종). NFLX 0.80·KLAC 1.04 EPS는 **10:1 액면분할**(원문 명시) 확인.
- 🏦 **영업이익 칸 개념 오염 6종 교정(2026-08-02, 비교 화면검증 2라운드)**: 금액 검산(원문 실재)은 통과하는데 **라벨이 다른 개념**인 값들이 영업이익 칸에 들어가 있었다 — JPM=Pre-provision profit(충당금 차감 전 30,706)·GS=Pre-tax earnings(8,563)·MS/C/AXP=세전이익·IBM=자사 고유 'Operating (Non-GAAP)' 조정 지표($3.3B). 비은행의 GAAP 영업이익과 한 줄에서 비교되면 왜곡(BAC·WFC만 빈칸이던 비대칭). → 프롬프트 규칙 "원문에 'Operating income/Income from operations' 라벨이 문자 그대로 있을 때만 채운다·pre-tax는 GAAP이어도 세전이익" + 6종 force 재요약 → 전부 빈칸. 부수 개선: JPM 매출이 managed(비GAAP 58,022)→**reported GAAP 57,347**로.
  - ⚠️ **1차 규칙은 GS·IBM이 저항했다** — JPM·MS·C·AXP는 따랐는데 GS는 같은 세전이익을 고집, IBM은 비GAAP 대신 GAAP 세전이익(2,479)으로 갈아탐. **실제 회사명·값을 박은 판정 예시**("골드만삭스 Pre-tax earnings 8,563 → 빈칸")를 넣은 2차에서 해소. 교훈: 개념 규칙은 추상 문장으로 안 먹히면 **그 모델이 방금 틀린 사례를 이름째 예시로** 박아야 한다.
  - ⚠️ **금액 실재 검산의 한계 확정**: 숫자가 원문에 있는지는 기계로 잡지만 **라벨 의미(영업이익 vs 세전이익)는 못 잡는다** — 이 클래스는 화면검증(사람이 표를 읽는 것)만이 방어선.
- ✅ **비교 화면검증 2라운드 전수 결과(2026-08-02)**: 미국 12종(AVGO·META·TSLA·LLY·JPM·MU·WMT·AMD·V·JNJ·MA·INTC) 금액+전년값 역산+EPS 전부 원문 대조 일치(META 전년 EPS 7.14·(13)% 원문 그대로 등) · 한국 16종 값·이익률 재계산·흑자전환 표기 일치, 전 50종 단위 이상 0건 · LRCX·KLAC·ANET·PLTR 전년 영업이익(2,046/1,327/858/174) 원문 실재 · MA(93억)·C(248억)·AMGN(27억·+125%)·HD(418억)는 **회사 prose 자체 반올림 표기 그대로**(각주 "회사가 원문에 쓴 값" 정합) · 신한지주 순이익 '1875원'으로 보인 것은 캡처 해상도 오독(포맷터가 1.875e11→'1875억원' 결정론 출력). 사소 잔여: MU 매출 +345%(정확히는 +345.8% — 회사 prose 반올림 유래, 1%p 미만).

## ⚖️ 신호 성적표 최종 — 비교 표의 잣대 통일 (2026-08-02, 완료)

최종 검증에서 화면 수치 **36/36 전부 일치**(표 3행·칩 건수·상세 카드 헤더·채점 대기·헤더 메타). 그런데 **내부 검산이 마지막 결함을 드러냈다** — `headOf`가 `win30 ?? winNow`라 **그룹마다 잣대가 달랐다**: 가치=30일 기준(81%·27건) / 이중확인·타이밍=현재까지(33%·57%).
- **문제 ① 행 간 비교 불가** — 같은 열에서 30일 성적과 현재까지 성적을 나란히 놓고 "어느 축이 나은가"를 읽게 만들었다. **비교 표의 존재 이유가 무너지는 결함**.
- **문제 ② 같은 행 안에서도 모수 불일치** — 가치 매수 표 14건(30일) vs 칩 4+13=**17건**(현재까지). 학생이 더하면 안 맞는다.
- **수정**: 칩·시장대비(`avgNow`/`avgBenchNow`)가 이미 현재까지 기준이므로 **전 행을 현재까지로 통일**(30일은 이중확인·타이밍에 데이터가 아예 없어 선택지가 아니었다). 30일 고정 성적은 상세 카드 '30일 후' 열에 그대로 존치. 표 부제에 *"모든 숫자는 신호일→오늘 기준으로 통일(세 축을 같은 잣대로 비교하려고) · 한 달 뒤 고정 성적은 아래 30일 후 열"* 명시. 수치 변화: 가치 매수 29%→24%(14→17건)·가치 매도 81%→90%(27→30건), 연동 문구도 하드코딩 제거.
- 검증: 모수 정합 `적중+빗나감=표본` **6/6**, 행 간 기준 동일.
- ⭐ 교훈: **비교하라고 만든 표는 잣대가 하나여야 한다.** 폴백(`A ?? B`)은 값이 없을 때를 메우는 편의처럼 보이지만, **비교 표에 쓰면 행마다 다른 것을 재게 된다** — 데이터가 없으면 폴백이 아니라 그 축을 빼거나 전체를 공통 잣대로 내려야 한다(같은 null 다른 폴백 사건의 '가로 비교' 버전).

## 🏷️ 신호 성적표 — 칩 '왜 몇 개만?'에 화면이 답하게 (2026-08-02, v9)

사용자 질문("왜 종목을 몇 개만 보여주나? 대표종목인가?")을 추적하니 **칩은 '신호일 최근순 3개'인데 화면 어디에도 그 말이 없었다.** 게다가 **같은 3개라도 뜻이 축마다 딴판**: 가치 매수는 적중 **4건 중 3개(사실상 전부)** / 가치 매도는 **27건 중 3개**. 건수 없이 3개만 두면 '대표종목'·'최고 성과'로 읽히는데, 실제로는 성과 순서도 아니다(매수 빗나감 −3.2/−6.7/−0.3).
- **수정**: `GroupStat.hitN/missN`(전체 채점분 기준) 신설 → `ChipRow`가 라벨을 **"매수 적중 4건"**으로 쓰고 칩 3개 뒤에 **"외 N건"**(호버: 최근순 3개까지·전체는 상세에서). 표 상단에 *"종목 칩은 신호일 최근순 3개(대표·최고 성과가 아닙니다)"* 명시. 캐시 v8→v9.
- 검증: `hitN + missN = n7` 및 `hitN/n7 = winNow` **6/6 정합**.
- ⭐ 교훈: **"상위 N개만 보여주는 목록"은 선정 기준과 모수를 함께 적어야 한다.** 같은 "3개"가 어떤 축에선 전수이고 어떤 축에선 1/9인데, 그 차이를 화면이 말하지 않으면 학생은 모든 축을 같은 무게로 읽는다(요약·발췌는 항상 "무엇의 일부인가"를 밝힐 것).

## ✅ 신호 성적표 최종 검증 (2026-08-02) — 표·칩이 미채점만 보던 결함

3차 배포본 화면검증. **수치는 원천 캔들 독립 재계산으로 전부 일치**(개별 이벤트 US 6/6·KR 7/7 — 수익률과 같은 기간 시장 수익률 모두 / 그룹 통계는 scored 절단이 없는 3그룹에서 적중률·평균·시장평균 완전 일치). 결함 2건은 뿌리가 하나 — **화면이 `recent`(최근 10건)만 보고 있었다**:
- **① 상세 표가 정보 0**: '가치 매도'·'타이밍 매수'는 최근 6건이 전부 경과 7일 미만이라 표 전체가 대시(—)와 D+N뿐. 학생이 펼쳐도 볼 게 없었다.
- **② 칩이 적중 종목을 대표 못 함**: 매도 적중 22건(81%)인데 칩 2개, **타이밍 매수는 31% 적중인데 적중 칩 줄이 통째로 소멸**.
- **수정**: `GroupStat.scored`(채점 완료 최신순 12) + `pendingN` 신설 → 표·칩 소스 교체, 표에 적중 ✓ 표식, 하단 "채점 대기 N건"(미채점을 숨기지 않되 정보 없는 행으로 표를 채우지 않는다). 칩에 **신호일 병기**(같은 종목이 다른 날 다른 상태일 때 — TI 07-22 채점 −6.3% vs 08-01 대기 — 칩과 표가 어긋나 보이던 혼동 제거) + ticker dedupe(STATE STREET 2번 → 서로 다른 3종목). 캐시 v8.
- ⭐ 교훈: **"최근 N건"으로 화면을 채우면 신호가 익기 전엔 화면이 텅 빈다.** 시간이 지나야 값이 붙는 지표(채점·성숙도)를 다루는 화면은 **'최근순'이 아니라 '값이 있는 것'을 기본 소스로** 삼고, 대기 중인 건수는 별도로 알려야 한다.
- 🩹 문구 정밀화: "매도는 시장 대비 +5~10%p" → 실측이 **+4.7~+10.1%p**라 타이밍 매도가 범위 밖이었다(실제 값 그대로 표기).

## 📏 신호 성적표 3차 — 시장 기준선 신설 "매수 29%가 왜 낮은가" (2026-08-02)

사용자 3대 지적(종목이 안 보임·'합류' 용어·매수 적중률 30%대라 학생이 신뢰 못 할 것). 세 번째를 **추측 대신 측정**으로 답하다 **진짜 결함을 발견**: 우리 backtest-autopsy 원칙이 *"승률은 baseline 대비 초과분만 의미 있다"*인데 **정작 이 성적표엔 기준선이 없었다** — 국면이 신호 탓으로 읽히는 구조.
- **실측(신호 기간 2026-06~07)**: **코스피 30일 후 상승 확률 0%(0/23)·평균 −16.6%·기간 −25.1%** / S&P500 73%·+0.5%. 그 구간 한국 시장은 **어느 날 사도 30일 뒤 100% 물리던 장**.
- **구현**: `SigEvent.benchNow/bench30`(KR=^KS11·US=^GSPC 같은 창) + `GroupStat.avgBenchNow/winVsNow`. 지수 캔들 2개 추가 fetch뿐(getTechCandles 재사용 — 야후 심볼이라 둘 다 'US' 경로). 표 각 셀에 **"시장 대비 +N%p"** 병기. 캐시 v5→v6.
- 🚨 **화면이 나를 반박했다(이번 라운드의 핵심)**: 초안에 *"매수 29%는 나쁜 게 아니라 바닥 기준선 위의 숫자"*라 썼는데 배포 후 실측하니 **매수는 시장 대비 −2.3~−3.9%p(열위)**, 매도는 **+4.7~+10.1%p(우위)**. 바닥 기준선 위인 건 맞지만 **그 기준선조차 못 넘었다.** → 3단으로 정직화: ① 장 탓인 부분(코스피 0%) ② **그래도 매수는 시장을 못 이겼다·매도는 확실히 이겼다** ③ 왜 매수만 어려운가(30일은 가치 신호에 짧은 자 + 하락장에선 싼 게 더 싸진다 → 그래서 타이밍을 겹치는 '이중 확인'이 필요) + 매도 81%도 실력만은 아님(다 떨어지는 장)을 대칭 유지.
- **종목 실명 노출**: 표 각 축에 매수 적중/빗나감/매도 적중 칩(최근 3개, 호버 시 신호일·수익률·같은 기간 시장). 상세 표에 '시장' 열 추가. → "그래서 뭐가 맞았는데?"에 바로 답(허전함 해소).
- **용어 '합류' → '이중 확인'**. ⚠️ 사용자가 제안한 **'통합'은 이미 통합추천(unified-reco)이 써서 충돌**하므로 회피 — '이중 확인'은 뜻이 자명하고 앱 내 다른 용어와 겹치지 않는다.
- ⭐ 교훈 1: **기준선 없는 승률은 국면을 신호 탓으로 돌린다.** 백테스트엔 baseline 원칙을 적용해놓고 정작 운영 대시보드엔 빠져 있었다 — 스킬에 적힌 원칙이 화면에도 적용됐는지 별도로 확인할 것.
- ⭐ 교훈 2: **낮은 숫자를 변명하는 것보다 정직한 게 신뢰에 낫다.** "매수는 아직 시장을 못 이겼다"를 그대로 쓰고, 대신 *왜* 그런지와 *그래서 이중 확인이 필요하다*로 연결했다. 앱이 자기 약점을 숨기지 않는 것이 이 화면의 존재 이유다.

## 📊 신호 성적표 2차 단순화 — 카드 6개 → 표 1개 (2026-08-02)

사용자가 같은 요청을 반복("아직도 복잡·궁극의 기준 하나로") → **1차(히어로/접기 분리)는 부족했고 두 가지가 더 틀렸다**:
- **① 히어로로 올린 ⭐합류가 정작 표본 1건·3건.** 같은 페이지가 "10건 미만은 통계가 아니라 일화"라 경고하면서 **가장 근거가 얇은 그룹을 크게 띄우고, 유일하게 통계적인 매도(n=27)를 접었다.** 자기모순.
- **② 카드 6개 구조 자체가 무겁다** — 카드마다 큰 % + 평균 2개 + 칩 2개 + 8행 표. 접든 안 접든 "읽을 게 6덩어리".
- **③ 정체성 혼동이 사용자 표현에 드러났다** — "3가지 매수/매도 **기준**". 이름은 성적표인데 판단 도구로 읽혔다(실제 판단은 종합 매수 판정·매매 브리핑 담당).

**재설계 = 결론 배너 + 비교 표 1개 + 접힌 상세.** 표 3행(⭐합류/🤖가치만/🚦타이밍만) × 매수·매도 적중률 — **6카드를 한 표로 접으니 축 비교가 처음으로 한눈에** 된다(소표본 ⚠️는 셀 안에 유지해 합류 3건·1건이 그대로 드러남). 상세는 `<details>`(지우지 않는 이유 = 존재 이유가 정직한 자기 채점).
- 🚨 **작성 중 스스로 잡은 오류(이 세션 내내 잡아온 바로 그 유형)**: 배너에 *"하나만 뜬 신호는 표에서 보듯 반쪽"*이라 썼는데 **정작 표는 가치 매도 81%(27건)가 페이지 최강 근거** — 문구가 숫자에 반박당했다. 배너에서 그 주장을 빼고, 표 하단에 **지금 표가 실제로 말하는 것**을 그대로 적었다: 합류는 증명 전 / 매도는 가치가 최강(단 하락장 효과) / 매수는 세 축 모두 30%대. **합류 우선의 근거는 이 표가 아니라 별도 백테스트**(눌림목 단독 −0.45%p vs 구조와 겹침 +1.8%p)임을 명시.
- ⭐ 교훈: **"우리 원칙이 옳다"는 주장과 "우리 데이터가 그걸 증명한다"는 주장은 다르다** — 원칙은 백테스트가, 이 페이지는 현재 표본이 뒷받침한다. 둘을 섞으면 화면이 스스로를 반박한다.

## 🏆 신호 성적표 1차 단순화 시도 (2026-08-02) — 히어로/접기 분리(부족했음)

사용자 피드백("3가지 매수/매도 기준이 굳이 필요한가? 궁극의 기준 하나로 심플하게")을 정면 수용. **근거는 화면 자체에 있었다** — Jarvis 매수 29%·타점 매수 31%처럼 반쪽 신호는 성적도 반쪽인데, 합류("이걸 우선하세요"라고 ⑤에서 이미 선언)와 같은 시각 등급으로 6카드 병렬 → "그래서 뭘 믿으라는 거야". 재설계:
- **🏆 히어로 = ⭐합류 매수/매도 2카드만**(가치+타이밍 두 엔진 합의 = 앱이 정의한 궁극 신호) + 행동 동선 한 줄("합류 종목은 매매 브리핑·종합 매수 판정에서 확인 — 여기는 성적 기록이지 매매 화면이 아님").
- **Jarvis·타점 단독 4카드 → 접힌 '🔬 엔진별 세부 성적'으로 강등.** 삭제하지 않은 이유 = 이 화면의 존재 이유가 정직한 자기 채점(재료 성적을 숨기면 안 됨). 대신 프레임을 뒤집어 교육 자료화: **"반쪽 성적이 나쁜 건 고장이 아니라 합류만 보라는 근거"**.
- 읽는 법 5항목 → 3줄(성적표 뜻 · 합류 하나 · 소표본/하락장 주의). API·채점 로직 불변(화면만).
- ⭐ **내부자 레이더 v4 원칙의 3번째 적용 확정**: 관측 화면은 행동 가능한 기준 하나를 헤드라인으로, 나머지는 전부 배경으로 — 엔진이 여럿인 건 내부 사정이고, 학생에게 병렬로 보여주는 순간 "어느 것을 믿을지"라는 새 문제를 만든다. 다중 엔진 화면 설계 시 기본값으로 삼을 것.
- 검증: 배포 HTML에서 신구조 확인(새 읽는 법 존재·옛 5항목 제거 — 히어로·세부는 데이터 로드 후 렌더라 SSR HTML엔 없는 게 정상). 브라우저 페인 세션 멈춤으로 최종 눈 확인은 사용자 몫.

## 📋 신호 성적표 화면검증 라운드 (2026-08-02) — 역주행 칩 모순·합류 자기상관

사용자 스크린샷 검증. 전 그룹 수치는 API 대조 일치(SELL 81%=22/27·−10.7/−20.4·대한광통신·ALIGHT / BUY 29%=4/14 / 타점 57%·31% / 합류 100%·25% — 소표본 ⚠️배지 정상 작동). 수정 2건(v4→v5):
- **역주행 칩 모순(표시 버그)**: 합류 매도 표본 1건(TEMPUS −10.4%)에서 best=worst가 되어 같은 종목이 '최대 방어 −10.4%'와 '역주행 −10.4%'에 나란히 — 하락은 매도 **적중**인데 역주행(반대로 감) 칩에 등장하는 자기모순. → 칩에 방향 가드(방어=하락만·역주행=상승만·최고 적중=상승만·빗나감=하락만). 로직 표 6/6. ⭐ 교훈: **best/worst 칩은 '순위'가 아니라 '라벨 뜻'이 성립할 때만** — 표본이 전부 한쪽으로 가면 최솟값도 승리인데 패배 라벨을 달게 된다(pearl 버딕트 문구 사건과 같은 클래스: 문구·라벨은 전 조합에서 참이어야).
- **합류 이벤트 자기상관(통계 오염)**: dedup이 같은 날짜만 걸러 타점 트리거 연일 재발화 시(OXY 07-28·30·08-01) 같은 Jarvis 런에 3번 합류 — '드물게(귀하게)' 표방 그룹에 같은 통찰 3표, 익으면 n=4급 소표본에서 3중 계상될 상황. → Jarvis 런 압축과 동일 원칙(종목·방향별 7일 내 재발화=첫날 1건). 라이브: 9건→6건·OXY 1건·NVDA도 런 첫날로. ⭐ **파생 이벤트 스트림(합류)은 원천 스트림(Jarvis)의 압축을 상속하지 않는다** — 원천에서 자기상관을 제거해도 다른 스트림(타점 전환)과 조인하는 순간 되살아나므로 조인 결과물에 다시 걸어야 한다.

## 📺 '클로드 코드 1000시간 꿀팁' 영상 검토 (2026-08-02) — 전 항목 기구현·수렴 확인

유튜브(Jay Choi 8분·팁 4종) `/watch` 전사 검토. **채택 0건 — 넷 다 이 프로젝트가 더 깊게 제도화한 것들**이라 ADR로만 기록(재검토 방지): ① 예스맨 반박("죽여보고 만들기") = Phase 0 실측·backtest-autopsy("기각시키기 위한 절차")·Jarvis 아첨 방지 가드 — **영상이 추천한 '카파시 스킬'(2:41)은 사용자 전역 CLAUDE.md 1~4장 그 자체**(같은 karpathy-guidelines MIT 소스, 이미 설치된 셈) ② 증거 기반 검증(완료 보고 불신) = 독립 재계산·화면검증·"배포 성공 ≠ 내 코드 배포됨"(관련 기록 98건) — 오늘 세션만 화면검증 3라운드가 실버그 9건을 잡음 ③ 컨텍스트 인수인계 = docs/context-notes.md 제도("기억력 하루짜리 천재 컨설턴트" 비유까지 하네스 영상의 ADR론과 동일) ④ 채점 가능 성공 기준·병렬 위임 = 전역 §4 Goal-Driven + 크론 자가치유(기계 검사 가능한 재시도 조건). 영상 자체는 입문·중급용으로 건전(하이프 없음) — 방향 수렴이 우리 시스템의 외부 검증인 셈.

## 📄 주간 리포트 화면검증 라운드 (2026-08-02) — 테마 ETF 광역 은닉·환노출 과소

사용자 스크린샷 검증. 수치는 원천 재계산 일치(전환율 스프레드 4지역 소수점·기여도=비중×주간·상위3 34.7·반도체 직접 20.3·수급 최근일 +7.24조/5일 −0.06조·이슈 3건은 오늘 자 마켓 카탈리스트 캐시 원문 그대로 = SSOT 중계 정상). 수정 3건(common v11·me v6):
- **⑨ 미등록 ETF '광역 ETF(분산)' 일괄 폴백(실질 버그)**: 신형 코드 테마 3종(1Q미국우주항공테크·TIGER코리아AI전력기기·TIGER코리아원자력, 주간 −3~−12.5%)이 광역에 섞여 '광역 분산 −2.3%'로 표시 — **역산하면 진짜 광의지수 3종은 −0.04%(보합)**. 분산이 잘 버텼다는 정보가 통째로 뭉개짐. → 미등록 ETF는 `classifyAssetRole`(portfolioRole SSOT — win-lose 학교 보드와 동일 체인)로 광역/채권/테마 분별. 실제 컴파일 lib 검증 8/8. ⭐ 교훈: **집계의 '기타/기본' 버킷에 라벨을 붙일 땐 그 라벨이 거짓말이 되지 않는지 봐야 한다** — '광역(분산)'은 성격 주장이라 기본값으로 쓰면 안 되는 이름이었다(미분류 정직 표기 원칙의 역방향 사례).
- **⑩ 환노출 29.2% '적정' 과소**: 통화 USD만 세서 **국내 상장 미국 추종 ETF(+22.4%p — 원화 호가지만 기초자산 달러)**가 빠짐 → 실질 ~52% = '주의'로 판정이 뒤집히는 크기(커버드콜 X-Ray "수익률은 통화에 종속" 교훈의 리스크 게이지 버전). 이름 휴리스틱(미국|나스닥|S&P) 11/11 검증(일본니케이·차이나항셍 오탐 0).
- **헤드라인 "코스피 WTI 5.2% 급락"**: 두 자산을 구분 없이 이어붙인 AI 제목 → 프롬프트에 '주어 하나·병렬 시 · 구분' 규칙.
- 비수정 판정: "삼성전자·SK하이닉스 20퍼센트대 급등"·"스페이스X IPO 데뷔" 이슈는 오늘 자 카탈리스트 캐시가 고른 뉴스 헤드라인 원문(주간 수익률 +5.2%/−2.3%와 어긋나 보여도 뉴스가 다룬 기간이 다른 것 — 리포트는 SSOT 중계가 정직).
- ⚠️ bt13 패턴 보강: 별칭 패치 + **`NODE_PATH=프로젝트/node_modules`**로 temp 컴파일본이 프로젝트 의존성(supabase 등)을 해석 — lib 단위검증 재사용 레시피.

## ⚔️ 승패 해부실 화면검증 라운드 (2026-08-02) — 글로벌 유니버스가 드러낸 국적 오염

사용자 스크린샷 검증. 수치는 전수 일치(그룹 276/143/244·평균 +6.0/−6.5·9요인 — **PEG는 lib 클램프 0<peg<3.5를 재현해야 1.48/1.56이 나온다**, 나이브 평균 5.06은 검증 스크립트 쪽 오답). 비수정 판정 2건 — 부동산 행 없음 = `total>=3` 최소 표본 필터의 의도된 생략(유니버스 리츠 1종) · 니케이225 '테마·기타' 칩은 portfolioRole(CORE_INDEX) 변경이 리밸런싱 자산군 배분에 영향이라 보류. 수정 3건(v8→v9):
- **🌍 국적 오염(실질 버그)**: 유니버스가 글로벌(EU 82·JP 30·CN 30종)로 확장됐는데 win-lose는 옛 `market` 이분법을 그대로 써 — ① **케링(KER.PA)이 상승 Top8에 'US' 칩**(승자 276종 중 접미사 비미국 티커 67개 전부) ② "미국 종목 비중 87%" 요인도 유럽·일본이 미국으로 집계. → `WLRow.origin`(US/KR/EU/JP/CN — EU_TICKER_SET 등 기존 SSOT 재사용) + 요인 라벨 '해외 종목 비중' 정직화(계산 불변·브리핑 교훈 문구 공유 lib라 자동 전파). 실측: 순수 US는 663종 중 332뿐.
- **푸터 '514종' 죽은 리터럴**(헤더는 663종 동적) → 동적화. "통합추천 전체(12종)" 사건과 같은 클래스 — **화면에 개수를 쓰면 반드시 데이터에서 뽑는다**.
- ⭐ 교훈: **유니버스·데이터 모집단이 확장되면 그 필드를 '국적·시장'으로 읽던 하류 화면을 전수 재점검해야 한다** — `market:'US'`(='한국 아님')의 의미 표류는 실적 리포트 일본 종목 사건(8/1)에 이어 두 번째 발현. origin 분기가 필요한 소비자는 hi52Radar·unified-reco·win-lose로 늘고 있다 — 새 화면이 market을 국기로 그리면 의심할 것.

## 🧰 '하네스 걷어내기' 영상 검토 — 층위 분리 판정 (2026-08-02)

유튜브(메이커 에반 「아직도 하네스 쓰세요?」 27분) `/watch` 전사 분석. 주장 = 최신 프런티어 모델(Opus 5·Fable 5·GPT 5.6)이 지시준수·긴맥락·자체기획·불확실성 인정을 흡수했으니 **실행 하네스(프롬프트 체인·규칙 더미·더미 멀티에이전트·퍼지 자체검증 루프·오픈소스 프레임워크·불필요 RAG 조각내기)는 걷어내고, 기획 하네스(문제정의·판단기준과 제약·ADR)만 남겨라**. 우리 시스템을 **두 층위로 분리해 판정** — 뭉뚱그리면 틀린다.
- **① 앱 런타임 LLM(Gemini flash-lite 무료 티어) → 영상 전제 자체가 불성립. 하네스 전부 유지.** 영상이 말하는 모델은 프런티어인데 우리 런타임은 정확히 영상이 "하네스가 필요했던 시절"이라 부르는 체급. **같은 날 세션이 반례 3개를 실측으로 제공** — JNJ "이십오조"(1,000배)·GS가 추상 규칙을 무시하고 세전이익 고집(이름 박은 예시로만 해소)·프롬프트 예시조차 무력했던 XOM(원인은 입력 절단). 게다가 영상 스스로 인정한다: *"기준이 명확할 때는 검사가 여전히 좋아요 — 표에 빈칸이 있는지, 답이 딱 떨어지는 것"*(13:08) — summaryIssues·amountIssues·ko() 후처리가 정확히 그 부류(결정론·원문 대조·LLM이 LLM을 심사하지 않음). 영상이 비판하는 '퍼지 자체검증 루프'("잘됐는지 봐"→밋밋해짐)는 우리에게 없다. 재요약 트리거도 결정론 검사에 걸린 것만 + "다시 하면 고쳐질 것만"(WFC 무한루프 교훈) — 영상 결론과 정합.
- **② 개발 워크플로 → 이미 영상의 결론에 독립 도달해 있었다.** CLAUDE.md 3,975줄의 실체 = 기각 ADR 93건 + ⚠️ 외부 세계 함정 기록 362건 — 영상의 "남겨야 할 기획 하네스"(문제정의·판단기준·ADR)와 동형. 영상의 규칙 다이어트 기준("왜 넣었더라 기억 안 나면 삭제 / 옛날 모델 실수 보완이면 삭제 / 우리 판단 기준이면 유지")을 적용해도 — 우리 ⚠️는 대부분 **모델 약점이 아니라 외부 세계의 사실**(DART 셀 구조·ECOS 누계·R-ONE CLS·Next Data Cache·야후 스케일 규약)이라 모델이 좋아져도 안 사라진다. 더미 멀티에이전트·버스·프록시는 이미 기각했고(기록 3건), Codex·Gemini는 역할 분리 + 실적 있는 독립 리뷰("자기 코드의 사각지대는 자기가 못 본다" — 영상도 11:51 "멀티 에이전트가 여전히 맞는 자리" 인정). 훅·스킬은 결정론 검사(캐시 키 reader)와 절차 지식 — "문서에만 있는 원칙은 안 지켜진다"는 우리 실측(색상 하드코딩 645→885)이 근거.
- ✅ **채택 2건**: ⓐ feature-kickoff plan.md 목적 템플릿을 영상의 문제정의 형식으로 강화("누가·언제·무엇을 못 해서·무엇을 포기하는가" + 소원 금지 + 모든 제안을 이 문단에 대보기) ⓑ **신규 규칙 추가 시 상시 질문 채택**: "이 규칙은 모델 약점 보완(실행 — 모델 세대 바뀌면 삭제 후보)인가, 맥락·판단기준·외부 사실(기획 — 영구)인가" — 실행성 규칙엔 그 사실을 주석으로 남겨 미래의 다이어트를 가능하게.
- ⛔ **기각**: 런타임 프롬프트 다이어트(모델 체급이 전제 미달·같은 날 반례 3개) · MAX_INPUT 통째 확대(30k→100k "그냥 넣어라"는 무료 티어 TPM과 크론 배치에 부담 — 손익계산서 앵커 발췌가 이미 더 정밀한 해법) · CLAUDE.md 대수술(§3 — 깨지지 않은 걸 고치지 않는다·모든 항목이 이미 '왜'를 갖고 있어 다이어트 기준을 통과).
- ⭐ 교훈: **"하네스를 걷어라"는 조언은 모델 체급·검사 성격(퍼지 vs 결정론)·규칙 성격(약점 보완 vs 외부 사실)을 구분해야 옳게 적용된다** — 우리 하네스의 대부분은 영상이 "버려라"는 것이 아니라 "남겨라"는 것이었다.

## 🎯 매매 브리핑 화면검증 라운드 (2026-08-02) — 보호 문구 잘림·교차 주입 2건

사용자 스크린샷 검증. **④⑤ 전 수치는 SSOT 재계산 완전 일치**(승패 246/282·12-1 −2.2/77.1·변동성조정 −0.01/1.04·온도 61→신중→현금 20~30%→MoS 24=20+11×0.4 검산·KB 배당 4,367·breadth 61.2/27.1). 비수정 판정 2건 — 💰권장 47만원 동일값은 **티어제 설계 정상**(85+→2.5%/78+→2.0%/이하 1.5%인데 오늘 Top5가 83·81·78·78·78로 전부 2.0% 티어) · 삼성E&A 🔥쌍끌이 배지는 **금요일 실데이터**(기관 5일 순매수 +766K 전환 — 전일 검증 때의 '단독 매집'에서 데이터가 실제로 바뀜). 수정 3건:
- **🛡 보호 문구 잘림(실질 버그)**: ② 사유 90자 하드컷이 PLTR "단, 현재 −22.7% 손실 중 — …"에서 끊겨 **"일부만 축소, 전량 투매 금물"이 사라짐** — 앞부분만 남으면 정반대(당장 팔라)로 읽힌다. 앞만 자르고 🛡 꼬리는 통짜 앰버 강조 보존. ⭐ 교훈: **요약 뷰의 문자열 컷은 '보호·반전 절'이 어디 있는지 모른 채 자르면 메시지를 뒤집는다** — 마커(🛡) 기준 분할로 자를 것(브리핑 트림 마크다운 리터럴 노출과 같은 클래스).
- **📅 실적 D-day 교차 주입**: 같은 페이지에 "PLTR 줄일 것"과 "PLTR 실적 D-1"이 나란히 뜨는데 서로 몰랐다 → ② 카드에 D-7 이내 어닝 칩 병기("📅 실적 D-1 · 갭 주의"). ③ 담을 것은 비보유라 캘린더 범위 밖(정직 생략).
- **⚖️ 분류 모순 맥락 노트**: ②가 "경기순환주 비중 과다(37%) 축소"라는데 ③ Top5에 경기순환주(셰브론·에퀴노르)가 다시 등장 — ③은 시장 전체 랭킹(보유 제외)이라 내 분산을 모르는 **축 차이**. 과다 분류와 추천 분류가 겹칠 때만 "더 담기보다 교체(줄인 자리 채우기) 관점" 노트 자동 표시. **같은 종목/분류가 두 표면에 동시에 뜰 수 있으면 축 병기 — 6번째 사례**(ETN 융합·타점×펀더·XLE 타점×로테이션·intact×SELL·급락 문구에 이어).

## 대시보드 화면검증 라운드 (2026-08-03) — 결함 9건 수정·배포

학생이 가장 먼저 보는 메인 화면. 실측으로 확정한 것만 적는다.

### 지수 '시가'가 사실은 전일종가 (가장 심각)
`market-indices/route.ts` 의 `meta.regularMarketOpen ?? prevClose` 폴백이 **100% 발동**하고 있었다.
야후 v8 chart `meta` 에는 지수용 `regularMarketOpen` 이 **아예 없다**(^KS11·^GSPC·^N225 실측 3/3 undefined).

결과로 갭 하락일엔 **시가 > 일중 고가**라는 물리적으로 불가능한 값이 화면에 떴다.

| 지수 | 구(폴백) | 실제 시가 | 일중 고가 |
|---|---|---|---|
| 닛케이225 | 64,362.02 | 63,834.95 | 63,905.12 ← 구값이 초과 |
| KOSPI | 6,595.45 | 6,358.27 | 6,393.00 ← 구값이 초과 |

실제 시가는 **같은 응답의** `indicators.quote[0].open` 첫 값에 있다 — 추가 호출 없이 교체.
배포 후 프로덕션 API 6/6 전부 일중 범위 안으로 복귀 확인.

### 자산 총액 차트 x축 날짜 중복
US·KR·코인의 타임스탬프를 한 Set 에 담고 slice(-30) 해서 **"30일"이 아니라 "30개 시각"**이 됐다.
장 시각이 시장마다 달라 같은 날짜가 여러 칸을 먹었다(화면에 8.1 이 3칸, 8.3 이 2칸).
KST 달력일로 접어 하루 1점만 남김. 3시장 재현: 구 30점=**11일**·중복 19 → 신 30점=**30일**·중복 0.

### 라벨이 다른 값을 가리킴
- `30일 수익률` 이 실제로는 매수가 대비 전체 수익률이었다 → `전체 수익률` 로 정정하고,
  차트 기간의 실제 변화율을 `기간 변화` 로 **새로 병기**. `30일 최고/최저` → `기간 최고/최저`.
- `fmtKrw` 가 만 단위 반올림이라 월 배당 13,500원이 `₩1만`(-26% 오차) → 옆의 `연 ₩16만` 과
  학생이 검산하면 **33% 어긋났다**. 100만 미만은 원 단위로 쓰는 `fmtKrwFine` 도입(₩13,500 · 연 ₩162,000).
- 월별 막대는 높이가 금액인데 라벨은 %뿐이라 -23.5% 달이 -17.5% 달보다 막대가 작아 보였다 → 라벨에 금액 병기.

### ⛔ 일방적 매도 강요 금지 위반 2건
**리밸런싱 처방** — coreIsOver 만 보고 항상 "일부 매도(수익 확정)" 고정이었다.
이 학생의 과잉 진영(Satellite)은 XRP -53.5%·SPCX -48.5%·인텔리안테크 -47.4%·원익IPS -38.9% 등
대부분 손실이라 **익절이 아니라 손절 강요**였다. 과잉 진영의 손익 구성으로 3분기 —
(수익으로 충당 가능 / 부족 / 전무), 수익 종목 이름과 모수(수익 N·손실 M)를 함께 노출해 문구를 검산 가능하게.
전무일 땐 "팔면 수익 확정이 아니라 **손실 확정**"이라 쓰고 신규 자금 배정을 권한다.
존재하지도 않는 자산을 주장하던 "Core(채권·ETF)" 하드코딩도 제거(이 포트폴리오 Core에 채권 0개).
미국 빅테크가 정리 후보로 뜰 수 있어 "**비중 조절이지 종목 판단이 아니다**" 캐비엇 추가.

**텐배거 문구** — returnPct < 0 한 버킷이라 **-0.1% 와 -48.5% 가 같은 메시지**를 받았다.
같은 화면 급락 경보("분산 전략 재검토 권장")와 정면 모순. 손실 깊이 4단으로 나누고,
위로 대신 **원금 회복에 필요한 상승률**을 보여준다 — XRP -53.5% → +115%, SPCX -48.5% → +94%(검산 7/7).

### 경고·색
- **코인 비중 11.7%** 가 앱 원칙(권장 5%·≥10% 위험)과 무관하게 2% 포트폴리오와 같은 색·서식이었다.
  가드는 CoinLab 에만 있고 대시보드엔 없었다 → 밴드 반영(🚨 위험 / ⚠️ 초과 / 이내).
- **MARKET DIRECTION 이 무가중 개수** — KOSPI -5.12% 폭락일에 "📈 4개 상승 우위"(빨강).
  상승 4개 합 +4.67%p vs 하락 2개 합 -6.06%p 로 **부호가 반대**였다.
  평균 등락폭을 항상 병기하고, 개수와 부호가 어긋나면 "⚠ 개수는 상승 우위, 폭은 반대"로 드러낸다.
- **수급 칩만 미국식 색** — +는 초록/-는 빨강이라 -6%가 빨강으로 찍혀 바로 위 보유표(한국식)와 **정반대로 읽혔다**.
- 카탈리스트 뉴스가 when:1d/2d 라 오늘 시세와 반대일 수 있다(오늘 헤드라인 "SK하이닉스 급등" vs 실제 **-8.79%**).
  ※ 최초 보고 때 -13.71% 라고 적었으나 그건 **야후 조정가** 기준이었다. KR SSOT 인 네이버는 -8.79%(전일종가 1,718,000).
  결론(헤드라인이 시세와 반대)은 그대로지만, KR 등락률을 야후로 재면 틀린다는 걸 다시 확인했다.
  캐비엇에 명시.

### 오탐으로 기각한 것 (기록해 둔다)
TIGER 200 당일 **-9.3%** 가 야후 -7.63% 와 달라 의심했으나, 네이버 원본이 **-9.28%**(전일종가 109,520)로
**앱이 맞았다** — 야후 쪽이 분배금 조정가였다. KR은 네이버가 SSOT라는 제2원칙대로.
총자산·손익·수익률·종목수·수익10/손실12·텐배거 5/9·평균 -11.9% 는 표 원본에서 재계산해 전부 일치.

### 캐시
`market-indices` 는 app_cache 가 아니라 **in-process Map**(TTL 5분)이라 새 배포 시 자동으로 비워진다 → 키 범프 불필요.
나머지 변경은 전부 클라이언트 렌더링.

커밋 `69e09c5` + `3982410` · 배포 후 프로덕션 API 실측으로 6/6 확인.
⚠️ **대시보드 화면 자체는 눈으로 확인하지 못했다** — 로그인 뒤라 브라우저 접근 불가.
리밸런싱 처방 3분기와 월별 막대 라벨 길이는 실제 데이터로 육안 확인이 남아 있다.

### 최종 라운드 (2026-08-03) — 배당 SSOT 통합 + 0% 과장 2건

**사용자가 잡았다** — KPI 카드는 ₩13,035 인데 상세 모달 TOTAL 은 ₩1만.
포맷 문제가 아니라 구조 문제였다. 같은 월간 배당 합계를 **4곳에서 따로** 계산 중이었다
(카드 monthlyDividend / 모달 행 목록 / 모달 월합 / 모달 연합). 모달 2곳은 `live(inv)` 대신
`priceMap` 을 직접 봐서 계산 근거까지 달랐다 — 제2원칙(SSOT) 위반.

`dividendCalc` useMemo 하나로 합치고 전부 파생. `annualDividend` 참조 5곳 → **1곳**.
교훈: **한 곳만 고치면 나머지 표면이 그 자리에 남아 오히려 모순이 드러난다.**
직전 커밋에서 카드만 `fmtKrwFine` 로 바꿨더니 모달과 값이 갈려 더 나빠졌다.

**0% 근처를 성과로 과장하던 곳 2건** (같은 성격이라 함께 처리)
- 히트맵 범례에 '보합'(회색)이 있는데 `getHeatmapColor` 에 그 분기가 없었다(`r >= 0` 이 전부 빨강).
  COP +0.04% 가 NVDA +8.4% 와 같은 빨강 — **범례가 거짓말**. ±0.5% 보합 밴드 추가.
  검산: 보유 11종목에 5개 밴드가 전부 실제로 배정된다(COP·XLP 회색 / KB금융·SK하이닉스 진빨강).
- 텐배거 문구가 음수는 4단인데 양수 0~100% 는 한 덩어리라 COP +0.04% 가 "1루 진출 성공!"을 받았다.
  ±0.5% 본전 구간 신설.

커밋 `28a5376` · 배포 후 지수 시가 6/6 회귀 없음, 배당 소스 5/5 정상.

### 사용자 질문으로 잡은 2건 (2026-08-03 밤)
- **월별 차트 우축이 음수를 raw 원 단위로**(-5040000) — 포맷터 `v >= 1e4 ? 만 : raw` 구조라 음수가 어느 분기에도 안 걸렸다. 좌축엔 음수 분기가 있어 좌우 단위가 달라 보였다. 부호 먼저 떼는 `fmtAxisKrw` 로 좌우 통일. (`164b672`)
- **"월별 평가손익"이 시계열로 오독** — 사용자가 "5~6월엔 플러스였는데 왜 5월부터 -507만?"이라 물었다. 데이터는 맞다: purchase_date 매수월 그룹핑 × 현재가 스냅샷이라 "5월 -507만" = 5월 매수분의 지금 손익. **계산이 아니라 라벨이 결함** — 제목에 "(매수월 기준)"이 있어도 x축이 "26년 5월"이면 시계열로 읽힌다(앱 주인이 오독했으니 학생은 100%). 제목 "매수 시기별 평가손익(현재가 기준)" + 부제 "그 달의 성과가 아닙니다" + x축 "…매수분" + 범례 "누적 합계". (`2232967`)
- 다른 학생 대시보드 정합성: 전 쿼리 `eq('user_id', uid)` + RLS, 하드코딩 티커·이름 0건, 빈 상태 가드 11곳 확인.

## 📈 진짜 월별 손익 시계열 (2026-08-04) — "이번 달 얼마 잃었나"
매수 시기별 스냅샷이 시계열로 오독되는 문제의 근본 해법 — 사용자 요청으로 진짜 시계열을 신설.
월말 스냅샷 DB 없이 **캔들 이력 재구성**: cum(M) = Σ qty×(close(M월말)−매수가)×fx(M월말),
원가도 그 달 말 환율로 환산해 마지막 달 누적 == 대시보드 평가손익(제2원칙 정합 — 매입일 환율 방식은 이 이유로 기각).
Phase 0 실측 14/14 · 신형 KR 티커 fchart 지원 확인 · SPCX(6/12 상장)는 상장 전 월 자연 제외 ·
크립토만 업비트 신규 수집 · 개인 로트는 POST body 로만(결과 무캐시 — 개인 계좌 경계).
스모크 검증: 7월이 최대 하락 월(-242만/4로트 표본) — "5~6월 플러스, 7월 폭락" 기억과 정합.
대시보드 토글 [월별 손익(시계열)](기본) | [매수 시기별]. 상세: docs/monthly-pnl/. 커밋 `1297fa6`.

## 실적 리포트 검증 라운드 (2026-08-05) — "스페이스X 실적이 왜 없지?"
사용자 질문 하나가 3중 결함을 드러냈다. SEC 실측으로 SPCX 가 26-08-04 에 8-K Item 2.02 를
실제 제출했음을 확정한 뒤 역추적.

1. **US 시총 파서 전면 오류** — 네이버 marketValue("1조 6,521억 USD")는 이미 USD 인데
   숫자를 이어붙여(16521) KRW 로 가정하고 ÷1,350. SPCX $12,238B(실제 $1.65T),
   NVDA $37,992B(실제 $5.1T) — **전 종목 오염**. 유니버스는 야후 시총이라 무사했다.
2. **"시총 상위 50"의 구멍** — 유니버스 미등재 신규 상장사는 8-K 를 내도 스캔 대상이
   아니었다. 보유 종목은 시총 무관 항상 수집하도록 크론에 단계 추가(heldAdded 9,
   SPCX 요약 "매출 78억 1,400만 달러 +92%" 화면 반영).
3. **가이던스 자릿수 무검사** — amountIssues 가 매출·영업이익만 봐서 AMD "약 13억
   달러에서 300만 달러 가감"($13B±$300M → 130억±3억)이 통과. 자유 서식 필드 검산을
   추가하니 **숨은 자릿수 오류 4종**(LRCX·AMAT·TEM·COHR)이 더 나왔다 — 크론이
   회차마다 자동 재요약. 오탐 3종(META·MS·MA)은 정수 billion·trillion·범위 표기
   인식으로 해소. 크론 3회 실행으로 AMD·META·MU·MA·CSCO·MS 플래그 해소 확인.

교훈: **검산기의 커버리지도 검산 대상이다** — 두 필드만 지키는 검사는 나머지 필드의
오류를 "통과"로 둔갑시킨다. 커밋 `839b3f5`.



## 화면검증 3순위 완결 — 종목 리서치·기술적 차트·AI운용본부·ETF투시경 (2026-08-05)

직접 브라우저·API로 4개 화면 전수 검증. 발견·수정 2건, 통과 다수.

**수정 ①** fast_grower 한글명 분열 — 리서치 화면에서 LynchAutoPanel 배지(SSOT
'고성장주')와 페이지 자체 매핑('빠른 성장주')이 한 화면에 공존. 11개 파일 30곳을
SSOT(LYNCH_CATEGORY_KR)로 통일. global-top10은 API가 한글 라벨을 보내는
writer/reader 짝이라 함께 교체. 커밋 `f2ecdd0`.

**수정 ②** 린치 분류 Matrix '7대' 표기 — 사이드바·밸류에이션은 '6대'인데 진단 탭
Matrix만 na(미분류) 버킷을 세어 '7대 분류'로 표기. 미분류는 린치의 분류가 아니라
데이터 상태다. 헤더+주석 4곳 정정. 커밋 `35f8327`.

**통과 확인**
- 리서치: NVDA 이익선 $196.20=EPS 6.54×30 정확, PEG 0.50 stock-info 일치,
  시총 $5,129B(파서 수정 하류 반영), SK하이닉스 cyclical 정상.
- 기술적 차트: NVDA·SK하이닉스·0117V0 캔들 OHLC 불변식 위반 0·날짜 중복 0,
  종가가 대시보드 현재가와 일치.
- AI운용본부: 계절 정합 70점이 hq-briefing·season-navigator·화면 3면 일치.
  통합추천 15종 점수 전수 검산 — 6축 가중합 + ROIC 가점(>=20→+3, >=15→+1.5)
  + FCF 방어 틸트(국면 발동, >=5%→+3, >=3%→+1.5)로 15/15 정확히 재현되고
  가점은 ⚙️/🛟 배지와 1:1 대응(투명성 원칙 준수). 매도 강요 없음(-41.7%
  종목에 "전량 투매 금물"), 자동매매 부인 문구 존재.
- ETF투시경: 11종 전부 resolved, 쌍둥이 지수 치환 정확(133690→QQQ·
  360750→SPY, 국내형은 null), 섹터 분해 합 75.2 = 직접주식 30.5+ETF분해
  27.3+잔여 17.5(기타 24.7 제외) 불변식 성립, 적자 ETF(ARKG)는 합산 PEG
  null로 정직 처리.

검증 방법 메모: dev의 무거운 페이지는 렌더러 45초 타임아웃이 잦다 —
fetch를 발사만 하고 window에 저장 후 폴링으로 회수. 단, 로그인 리다이렉트가
window를 초기화하니 정착한 페이지에서 발사할 것. 대시보드 하위 탭은
display:none 마운트라 innerText가 아니라 textContent로 존재 확인.


## 리밸런싱 화면 검증 + 실적 요약 플래그 종결 (2026-08-05)

사용자가 올린 리밸런싱 전체 스크린샷 전수 검산 + 관찰 항목 정리.

**수정 ③** 라벨 캐시 박제 — 상세 진단 '분산 개선'에 옛 라벨 '빠른성장주' 잔존.
f2ecdd0 라벨 통일 때 응답에 라벨을 싣는 두 라우트의 캐시 키를 안 올렸다(재발
함정 1위 재현). ai-rebalance v47→v48 · global-top10 v2→v3. 같은 라우트에서
부분실패 박제도 발견 — buysUnavailable(통합추천 fetch 실패) 결과가 24h 캐시돼
보강 카드가 하루 종일 자산군 2건뿐이었다. 실패 시 setCache 생략. 커밋 `f3228d4`.

**수정 ④** 실적 요약 금액 그라운딩 — 관찰 항목이던 플래그 4종을 원문 대조로
전수 판정하니 전부 진짜 오류였다(TEM $1.605B→"16억 5,000만" 오독, COHR 원문
"REVENUE OF $1.81B"인데 "18억 5,600만" 환각, LLY 주당 $35.50를 "35억" 총액으로).
경고 문구만으론 반복돼 원문 명시 금액 목록을 기계 추출해 프롬프트에 박았다
(statedAmountStrings). 검산기도 $1.81B 축약 표기 인식. 비실적 8-K(PLUG·OXY —
보유 종목 수집분)는 매출 수치가 원문에 없어 빈칸이 정직한데 정량필드 플래그가
매일 재요약을 헛되이 돌려, 80자 내 숫자 근접 조건으로 트리거를 껐다(5종 실측
5/5). 최종 stillFlagged [] — LRCX·AMAT·TEM·COHR·LLY·PLUG·OXY 전량 해소.
커밋 `c0c1cfd`.

**검산 통과** (스크린샷 전수)
- 모닝스타 평균 ★1.8 = (5+1+1+1+1)/5 정확(보류 4종 제외가 옳다), 비중 합 100.0.
- X-Ray 실질 노출 = 스타등급 비중 × 직접주식 30.5% + ETF 경유가 종목별로 정확
  (SK하이닉스 27.6%×0.305+1.8=10.2 ✓, NVDA·GOOGL·인텔리안테크 동일).
- 매매 플랜 산식: 주당리스크 396,000−346,028=49,972, 수량 31만÷49,972=6주,
  2R·3R 정확(±1원은 손절가 반올림).
- 통합추천 15종 + 지역 커버리지 5종 = 20종 점수를 6축 가중합으로 독립 재계산.
  원점수와 화면값의 차이가 전부 ROIC·FCF 가점 규칙과 정확히 대응(에퀴노르 +6.0
  = ROIC 49% +3, FCF 31.1% +3 / 오리온 +4.8 = ROIC 19% +1.5, FCF 8.1% +3 /
  메리츠금융 0.0 = 고ROE 배지라 ROIC 가점 없음). 배지 없는 종목은 차이 ±0.4 이내.

교훈: **라벨을 고치면 그 라벨을 응답에 싣는 캐시가 어디인지부터 찾아라** —
컴포넌트 30곳을 고쳐도 캐시된 API 응답 속 라벨은 그대로 살아 돌아온다.


## 리밸런싱 화면 검증 · 대시보드 성능 · 실적 요약 자릿수 (2026-08-05 밤)

### 1. 라벨 통일이 캐시에 막혀 화면에 안 나왔다
리밸런싱 상세 진단의 '분산 개선'에 옛 라벨 '빠른성장주'가 그대로 떠 있었다.
낮에 `f2ecdd0` 로 SSOT 통일을 했는데, 그 라벨을 응답에 싣는 두 라우트의 캐시 키를
안 올려 00:29 계산본이 24h 박제됐다. ai-rebalance v47->v48, global-top10 v2->v3.
**재발 함정 1위(응답 '내용'만 바뀌어도 키를 올려라)를 내가 그대로 밟았다.**
프로덕션 워밍 후 실측: 옛 라벨 0건 / 새 라벨 8건. 커밋 `f3228d4`.

### 2. 부분실패를 24시간 박제하고 있었다
같은 화면의 "통합추천을 불러오지 못해 개별 종목 보강이 빠졌습니다"(buysUnavailable)가
**그 상태 그대로 캐시**돼, 한 번 실패하면 보강 카드가 하루 종일 자산군 2건뿐이었다.
실패 시 `setCache` 를 생략하도록 수정 — fetch 시도 자체가 통합추천 캐시를 데우므로
다음 요청에서 온전한 결과로 수렴한다.

### 3. 왜 그 실패가 났는가 -> 대시보드가 API 42개를 동시에 쏘고 있었다
탭 50개가 `display:none` 으로 **전부 마운트**돼 숨은 탭의 useEffect 가 다 돌았다
(컴포넌트 61개 중 42개가 마운트 즉시 fetch — 정적 분석 실측). 대시보드를 한 번 열
때마다 보지도 않는 탭들의 API 42개가 경합했고, 통합추천 타임아웃의 배경이 됐다.

- **탭 지연 마운트**: `seenTabs` 로 한 번이라도 연 탭만 렌더. 연 뒤에는 유지되므로
  재방문 시 재조회 없음. 기본 탭 live 는 항상 마운트.
  이미 안쪽에 `{dashTab===X &&}` 가드가 있던 25개는 그대로, 가드가 없어 항상
  마운트되던 **24개**(macro 16·moneyflow 8·rebalance 6·globaltop10 6 등)가 새로 지연.
- **dynamic import 54개**: 지연 마운트가 없으면 숨은 탭도 렌더되어 청크를 받으므로
  `next/dynamic` 만으론 효과가 0이다. **둘은 한 세트**다.
- 실측: First Load JS **627 kB -> 330 kB**(라우트 JS 321->51.8 kB), 최초 마운트 탭 50->1.
  탭 방문->이탈->재방문에서 마운트 유지 확인, live 탭 정상, 런타임 오류 0.
- 함정: `dynamic()` 은 import 자리(파일 앞쪽)에서 **즉시 평가**된다 — `loading` 을 const
  화살표로 두면 TDZ 로 죽는다. 호이스팅되는 `function` 선언이어야 한다.
- 커밋 `6cc2d2b`(+토큰 정리 `17d7fdb`).

### 4. 실적 요약 자릿수 — 검산기가 옳았다(3/3 진짜 오류)
관찰만 하던 플래그를 원문 대조로 판정: TEM 은 "$1.595 to $1.605 billion"을 "16억
5,000만"으로(소수 셋째 자리 오독), COHR 은 "REVENUE OF $1.81B"인데 "18억 5,600만"
(원문에 없는 숫자 = 환각), LLY 는 **주당** 가이던스 "$35.50 to $36.50"을 "35억 5,000만
달러" 총액으로 옮겼다(새 유형).

- **금액 그라운딩**: 원문의 서술형 달러 금액(축약 `$1.81B` 포함)을 기계 추출해
  프롬프트에 목록으로 박고 "목록과 자릿수까지 일치"를 강제. 경고 문구만으론
  같은 오독이 반복됐다 — **모델에게 규칙을 주는 것보다 원문 값을 주는 게 낫다.**
- 검산기도 `$X.XB/$XM/$XT` 축약 인식, 주당 금액을 총액으로 바꾸지 말라는 규칙 추가.
- **비실적 8-K 재요약 중단**: PLUG·OXY 는 원문에 매출 수치 자체가 없어 빈칸이
  정직한데 매일 헛되이 재요약했다(영업이익과 같은 원칙). 단어만 검사하면 OXY 의
  법정 boilerplate("projections of earnings, revenue or ...")에 걸려, 80자 내 숫자
  근접 조건으로 판정(5종목 실측 5/5 일치).
- 결과: 강제 재요약 후 `stillFlagged []` — LRCX·AMAT·TEM·COHR·LLY·PLUG·OXY 전량 해소.
  커밋 `c0c1cfd`.

### 5. 다른 학생 대시보드 — 데이터는 깨끗, 문구가 거짓말
프로필 9명·보유 79행·거래 148건 전수 점검: 가격·수량·통화·시장·매수일 결측 0,
이상 통화 0, 6대 분류 외 값 0. 대신 **보유 0인 학생 3명**에게 월별 손익 카드가
"가격 이력을 불러오지 못했습니다 — 새로고침 해보세요"라고 말하고 있었다.
아무것도 실패하지 않았고 새로고침해도 달라지지 않는다.
보유 없음 / 재구성 중 / 실패 / 이력 짧음 **네 상태를 각각** 말하도록 분리하고,
실패를 상태(`pnlSeriesFailed`)로 남겨 취소 가드 안에서 함께 확정. 커밋 `85486eb`.

### 검증 환경 메모
- 세션 중 Bash 도구가 셸 초기화 단계에서 깨져(모든 명령이 unexpected EOF) PowerShell 로
  전환. Vercel 토큰은 PowerShell 쪽에 없어 `vercel whoami` 로 세션을 확인한 뒤 배포했다.
- 프로덕션 대시보드는 로그인 뒤라 **브라우저 실증을 하지 못했다** — dev 서버 실측 +
  프로덕션 빌드 산출물(번들 수치)로 대신했고, 이 한계를 그대로 남긴다.
- 전 라우트 스캔(27개): 200 또는 307(로그인 리다이렉트)만 — 런타임 오류 0.
  `src/app/actions` 는 서버 액션 파일뿐이라 404가 정상.


## 프로덕션 로그인 검증 라운드 — API 79종 전수 + 화면 순회 (2026-08-06 새벽)

로그인된 프로덕션 세션으로 나머지를 검증했다. API 79개 병렬 전수(75 정상 ·
3은 티커 필수 라우트의 정상 400 · kr-short 503은 "러너 미적재" 정직 안내) +
화면 순회(assets·analysis·reco-hub·briefing·history·watchlist·valuation·dividend·
hi52-radar·tech-screener·macro-hub·master-strategy·school-league·win-lose·
signal-report). 대시보드 지연 마운트도 프로덕션 실증(탭 50개 중 live 만 마운트).

### 발견 1 — 승패 해부실: 색 규약 위반 + 한 화면 두 잣대 (`f59d855`)
이 화면만 등락률이 미국식(초록=상승·빨강=하락)이라 같은 SK하이닉스 −28.8%가
대시보드에선 파랑, 여기선 빨강이었다. 한국식으로 통일(±0.5% 보합 회색).
또 유니버스는 ±3% 보합 밴드인데 '우리 포트'는 0% 이분이라 +0.0%가 '오르는',
−0.1%가 '내리는'에 들어갔다 — 같은 임계로 통일하고 보합 제외를 캡션에 병기.

### 발견 2 — 신호 성적표: 하드코딩 해설이 표를 반박 (`f59d855`)
해설 "매수는 세 축 모두 열위(−2~4%p)" vs 표 +1.5/+1.1/−1.6%p — 리터럴 숫자가
데이터가 흐른 뒤 거짓이 됐다. edgeOf() SSOT 로 표 셀과 해설이 같은 함수를 쓰고
우위/열위/혼재 방향 판정까지 데이터가 정하게 했다. 배포 후 실증: 해설
"-0.8%p~+3.0%p" == 표의 매수 3축 min~max 정확 일치, 매도 방향 문구도 데이터를
따라 "아직 안정적으로 이기지 못함"으로 바뀌었다(옛 하드코딩과 정반대 —
그게 위험했다는 증명).

### 발견 3 — 전 페이지 하이드레이션 불일치, 진범은 TopHeader 날짜 (`0ea2a31`)
스쿨 리그가 "데이터 집계 중…"에서 얼어붙었다. API 는 200(학생 9명)인데 화면
useEffect 가 실행조차 안 됐고, 콘솔에 React #425/#418/#423 — /assets 에서도
같은 4건 재현 = 공유 레이아웃 문제. TopHeader 가 렌더 중 new Date() 로 날짜를
찍는데, 서버(UTC)와 클라이언트(KST)의 '오늘'이 KST 자정~09시 사이 하루 어긋나
그 시간대의 모든 페이지 하이드레이션이 깨졌다. 무거운 화면은 복구가 실패해
초기 로더에 동결. 낮 검증에서 안 보인 이유이자, 학생들이 밤에 접속할 때만
터지는 시한폭탄이었다. 날짜를 마운트 후 렌더로 수정, 자정 직후(취약 시간대)에
배포·리로드로 오류 0건 실증.

### 운영 메모
- kr-short 503: 데이터가 비어 있다 — 선생님 PC에서 krx-short-runner.py 를 돌려야
  KR 공매도 축이 채워진다(코드 결함 아님, 러너 미가동 상태).
- 순회하지 않은 화면: weekly-report·bonds·guru-portfolio·school-lounge·
  real-estate 4종·investment-academy·admin — API 는 전수 정상이었고 화면은
  다음 라운드로(정직 기록).


## 🎩 거장 위원회 — AI Berkshire 4인 시스템 접목 (2026-08-06)

유튜브 쇼츠(QJC)로 소개된 오픈소스 "AI Berkshire"(GitHub 2,200+ — 버핏·멍거·
단요핑·리루 4인의 사고법을 스킬로 박제, 독립 분석->교차 반박->강제 결론)를 심층
분석(/watch 프레임+전사)하고 우리 앱에 재구현했다.

**원본과 다르게 만든 것(원칙 때문)**
- 원본은 LLM 4개가 판정까지 한다 — 우리는 **판정 전부 결정론 코드**(mastersCommittee
  SSOT), LLM 은 1회 호출로 발언·반박·의장 종합 '서술'만(판정·숫자 창작 금지 프롬프트).
- 원본 레드라인 8개 중 '경영진 정직성' 등 데이터로 판정 불가한 것은 뺐다 — 우리
  데이터로 판정 가능한 6개(이익-현금 괴리·ROE 부풀림·기저효과·적자+전망부재·
  기대 극단·검증 불가)로 재구성. 판정 못 하는 건 하는 척도 안 한다.
- 거울 테스트(5문장)는 판정 미반영 교육 장치로 강등(LLM 주관 판단 배제).
- 4개 병렬 LLM 대신 결정론 판정을 입력으로 1회 호출 — Gemini 무료 티어에서 비용 1/5.

**지킨 원칙**: 신규 수집 0(stock-info 23키 스케일 실측·stock-fcf·reverse-dcf·해자·
ROIC·기저효과·버핏DCF 전부 기존 SSOT), 6축 점수 미반영, 매수 관점 판정 캐비엇
(보유 불통과 != 매도 지시), 결측=warn(통과 아님), 부분실패 박제 금지(Gemini 실패 시
setCache 생략), 매수 밴드는 DCF 가능+레드라인 없음일 때만(가짜 정밀 금지).

검증: 3종목 판정 산식 손검산 12/12 · NVDA 회색(버핏 vs 멍거·단요핑 갈림 — 반박이
판정표 근거로만 생성) · 005930 4/4 통과 · CVX 회색(demanding fail·레드라인 미발동).
/research 새 탭. 커밋 `45992f1`.


## 거장 위원회를 매수 표면에 연결 + 💱 통화 단위 불일치 발견 (2026-08-06)

"거장 위원회가 그 탭에서만 보이냐"는 지적에서 시작. 판정을 매수 표면에 붙이면서
기존 앱의 **진짜 버그**를 하나 끌어냈다.

### 연결 방식 — 왜 점수에 안 넣었나
위원회 재료(PEG·ROE·FCF·기저효과)는 **이미 6축 점수에 들어가 있다**. 점수에 또 더하면
같은 신호를 두 번 세는 것이다. 대신 위원회가 보태는 건 다른 축이다:
① **레드라인(관문)** — 점수는 연속, 위원회는 통과/탈락. ② **얼마에 살 것인가**(안전마진 구간).
그래서 "통합 상위인데 위원회 불통과"가 나올 수 있고, 그 사실을 헤더에 먼저 적었다.

- `?brief=1` 모드 — Gemini(토론) 없이 판정만. **판정은 리서치 탭과 같은 computeCommittee**
  (화면마다 판정이 다르면 제2원칙 위반). 캐시는 full/brief 분리 — brief 결과를 full 키에
  넣으면 위원회 탭이 "토론 실패"로 24h 박제된다. 실측 콜드 5종 8.6초 / 웜 316ms.
- 붙인 곳: 통합 매수 추천 15종 카드, 리밸런싱 '보강할 것'.
- ⛔ '버릴 것·줄일 것'에는 안 붙였다 — 매수 관점 판정이라 불통과가 매도 지시로 읽힌다.

### 💱 통화 단위 불일치 — 합계 검산을 통과하는 스케일 오류
리밸런싱 화면 검증에서 **TSM 매수구간 $6,431~$7,809**(현재가 $414)가 떴다.
원인: `freeCashflow` 739,057,991,680 이 **TWD** 인데 `marketCap` 은 **USD** —
해외 상장·ADR 은 재무제표가 현지통화, 주가·시총이 달러로 온다.
결과: FCF/시총 34%, DCF 내재가치가 현재가의 **22배**(에퀴노르 19.7배).

부호·합계 불변식을 전부 통과하는 유형이라 **비율의 상식 범위**로 판별했다:
FCF/시총 > 30% 또는 내재가치 > 현재가×5 → `unitSuspect`.
→ 매수 구간 보류 + 버핏의 '현금 창출력'·'안전마진'을 warn(검증 보류)으로 강등 +
   화면에 이유 표기. **틀린 가격은 없는 가격보다 나쁘다.** 캐시 v1→v2.
검증: TSM·EQNR 보류 전환, NVDA(0.5x)·삼성전자(2.3x)·토탈(0.7x)은 그대로. 프로덕션 확인.

### ⚠️ 미해결 — 별도 작업으로 분리
같은 원인으로 **통합추천의 "💵 FCF수익률 35.1%(우수)"(TSM)·"31.1%(우수)"(에퀴노르)
배지가 부풀려져 있고, 그 값이 FCF 방어 틸트로 점수에도 들어간다.**
위원회는 가드로 막았지만 근본 수정은 `buffettDcf`·`stock-fcf` SSOT 차원이라 범위가 크다.
→ 다음 세션 1순위. 커밋 `c14346c`·`31f5f18`.


## 🎯 '핵심 추천' 시뮬레이션 — 매수 신호에 위원회 필터를 소급 적용 (2026-08-06)

"이중확인 승률 50%는 실망 — 이중확인 ∩ 위원회 통과만 핵심 추천으로 격상하면
승률이 어떻게 변하나"(사용자). signal-report 에 `?full=1`(scored 12건 캡 해제)을
추가해 매수 신호 47건(고유 43건·25개 티커)에 오늘의 위원회 판정을 소급 적용했다.

### 결과 (2026-06-02~08-06 · 하락장 국면)
| 필터 | n | 승률 | 평균 | 시장대비 |
|---|---|---|---|---|
| 전체 매수(기준선) | 43 | 42% | -3.6% | -0.5%p |
| + 위원회 **통과만** | **1** | 0% | -36.1% | — (공집합 수준) |
| + 위원회 **비불통과**(회색 허용) | 17 | **53%** | -1.4% | +0.4%p |
| + 레드라인만 제거 | 18 | 50% | -1.7% | +0.6%p |
| 이중확인 ∩ 비불통과 | 2 | 100% | +4.7% | +1.7%p (일화) |

- 필터가 **걸러낸 26건**: 승률 35%·평균 -5.0% — 걸러낸 쪽이 실제로 나빴다.
  그룹별로도 전부 개선(jarvis 39→50 · timing 44→56 · confluence 50→100).
- **'통과만'은 비현실**: 25개 티커 중 pass 1개(삼성전자)뿐 → 추천이 텅 빈다.
  게다가 그 1건이 -36.1% — **오늘 통과인 이유가 '그동안 떨어져서 싸졌기 때문'**
  (룩어헤드의 역설). 실전 티어는 '레드라인 없음 + 불통과 아님'이 맞다.
- **KR vs US 진단**(사용자 의심 검증): KR 절대 승률 24% vs US 54% — 직관대로.
  그런데 **시장대비는 정반대**: KR +2.3%p(시장 이김 59%) vs US -2.4%p(35%).
  KR 신호는 폭락장 속에서 시장을 이겼고 US 신호는 상승장을 못 따라갔다.
  즉 "KR 종목 선정이 나쁘다"가 아니라 "하락장에선 뭘 사도 절대수익이 마이너스"가
  본질 — 하지만 학생은 절대수익으로 느끼므로 사용자 우려는 실용적으로 타당하다.

### 한계 (정직)
- **룩어헤드**: 위원회 판정은 오늘 기준(과거 재무 스냅샷 없음). 가격 의존 체크
  (안전마진·기저효과·어닝일드)는 신호 시점과 다르다. 근사 시뮬레이션이다.
- 표본 43건·한 국면(하락장) — 핵심추천 교집합은 2건이라 "승률 100%"는 일화.
  말할 수 있는 것: 필터 방향이 3그룹 모두에서 일관되게 +8~11%p.
- 이 승률로 화면에 숫자를 박으면 가짜 정밀 — 도입 시 "표본 적립 중"으로 시작해야.

향후: '핵심 추천' 티어 도입 여부·기준(비불통과 vs 레드라인 제거)은 사용자 결정 대기.


## 🎯 확장 시뮬레이션 — 표본 1,089건, 그리고 역인과의 발견 (2026-08-06)

"표본 47건으론 판단이 어렵다 — 최대한 늘려보라"(사용자). 기존 백테스트 하네스
레시피(실제 lib 컴파일·재구현 0)로 **화면과 동일한 타점 SSOT**(timingFromCandles)를
유니버스 145종 × 2년 캔들 전 봉에 소급했다(봉 i 판정은 data[0..i]만).
신호 1,089건(green 진입 712 · 정예 377) · baseline 31,031봉(전방 20봉 평균 +2.51%).

### 1차 결과가 "반대"로 나왔다 — 그리고 그게 답이었다
위원회 불통과 군 +7.15%(초과 +4.64%p)가 비불통과 군 +2.41%(-0.10%p)보다 좋았다.
필터가 틀린 게 아니라 **시뮬레이션 방법의 역인과**다: 위원회 판정은 '오늘' 기준인데,
기저효과·기대과도·안전마진은 가격 파생이라 **"지난 2년 많이 오른 종목 = 오늘 고평가
= 불통과"**가 된다. 그 종목들의 과거 신호 성과가 좋은 건 당연하고 무의미하다.

### 역인과 분해 — 가격 파생 vs 회계 품질
레드라인을 갈라서 재측정(green / 정예):
| 군 | n | 초과 | 절사초과 |
|---|---|---|---|
| 깨끗(레드라인 0) | 352/205 | -0.10 / -1.39%p | -1.19 / -1.80%p |
| **회계 품질 나쁨**(이익-현금 괴리·ROE 부풀림) | 87/41 | **-0.49 / -2.93%p** | -1.22 / **-3.51%p** |
| 가격 파생 레드라인(기저효과·기대과도) | 238/119 | +6.33 / +4.22%p ← 역인과 | |

- **역인과가 약한 회계 품질 축은 일관되게 나쁘다** — 특히 정예 타점에서 깨끗 대비
  -1.5~-1.7%p 뒤지고, 분기 분해에서 하락장(2026Q2)에 격차 최대(-1.05% vs +3.21%).
  부풀린 이익은 상승장엔 용서되지만 하락장에서 대가를 치른다. → **이 레드라인은
  필터로서 실증 근거 확보**(표본 13종목 — 부검 2단 경계는 병기).
- 깨끗 군도 baseline을 못 이김(-0.1~-1.4%p) — 타점 edge가 상승 국면 전용이라는
  기존 재측정(2026-07-30)과 정합. 표본 기간이 하락장을 포함해서다.

### 정직한 결론
1. **위원회 '전체' 필터의 미래 효용은 소급 시뮬로 검증 불가** — 가격 파생 성분이
   수익률과 역인과로 얽힌다. 표본을 아무리 늘려도 이 오염은 커질 뿐이다.
2. 미래 효용의 최선 추정은 여전히 **47건 근접 시뮬(+8~11%p)** — 신호·판정 시점이
   2개월 내라 역인과 오염이 작다.
3. 진짜 답은 **전향적 적립**: 핵심 추천 티어를 만들면 앱 신호 성적표가 룩어헤드 0으로
   자동 채점한다. 승률 숫자는 그때까지 화면에 박지 않는다.

교훈(함정 등재): **오늘의 가치 판정을 과거 수익률 검증에 소급하지 마라** — 판정에
가격이 들어있으면 "많이 오른 것"이 "나쁜 판정"으로 라벨링되는 역인과가 생긴다.


## ⭐ 핵심 추천 티어 도입 (2026-08-06)

시뮬레이션 검토 끝에 사용자 승인("나중에 추가 수정하더라도 일단 진행")으로 도입.
통합 매수 처방 최상단에 **3중 검증 통과 종목만** 올리는 섹션.

기준: 6축 통합 상위(기존 선별) x 타이밍(진입 적기 green / 정예 타점) x 거장 위원회
비불통과(레드라인은 fail 을 강제하므로 non-fail 로 충분) x 통화 단위 의심 아님.
판정은 리서치 탭·배지와 같은 computeCommittee SSOT(brief 캐시 재사용 — 추가 계산 0).

정직 장치(전부 화면에 명시)
- 승률 숫자 미표기 — 근접 표본 2건뿐 + 소급 검증은 역인과로 무효 확인.
  "표본 적립 중 · 성적표가 매일 채점"으로 시작한다.
- 없으면 없다고 말한다("세 관문이 동시에 열리는 순간은 드뭅니다").
- 판정 결측 = 미통과. 모멘텀 크래시 국면이면 "핵심 추천이어도 분할·소액" 캐비엇.

검증: check+check:build 통과, 로컬 빌드 청크(391.*.js)에 섹션 코드 실림 확인,
같은 커밋 트리로 Vercel 원격 빌드·배포(exit 0·ready). ⚠️ **화면 실증은 못 했다** —
검증 도중 dev·prod 로그인 세션이 모두 만료됐고 자격증명 입력은 원칙상 불가.
로직 입력(타이밍 배지·위원회 판정)은 직전 세션에서 개별 검증된 값과 동일 소스.
다음 로그인 때 통합 매수 처방 상단에서 눈으로 확인할 것. 커밋 `73a3483`·`532b012`.
