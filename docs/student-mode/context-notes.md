# 학생 간단 모드 — 결정 기록

## Phase 0 실측 판정표 (2026-09-25)

| 항목 | 결과 | 판정 |
|---|---|---|
| 학생 등록 활동(선생님 제외) | 5월 종목 54·거래 54(4명) → 6월 3 · 7월 2 · 8월 3 · 9월 0 | 가입 때 몰아 넣고 끊김 |
| 한 번도 등록 안 한 학생 | 8명 중 3명 | |
| `auth.users.last_sign_in_at` | 비밀번호 로그인 때만 갱신 — 세션 유지 중 등록한 학생이 '107일 전 로그인'으로 나옴 | ❌ 접속 지표로 못 씀 → 접속 기록 신설 |
| 방문 분석 도구 | 없음(vercel analytics·posthog·gtag 0건) | 열람 여부 측정 불가 |
| 메뉴 | 사이드바 10그룹 34항목 · page.tsx 35개 · 로그인 착지 = 대시보드(4,027줄·탭 약 50) | |
| 매수 등록 폼 | 시장·티커·이름·매수가·수량·매수일·코어/위성 7칸, KR 은 6자리 코드 입력 | 이름 검색 없음 |
| 이름 검색 API | 앱에 없음 | 신설 필요 |
| 네이버 자동완성 `ac.stock.naver.com/ac?q=&target=stock` | "삼성"→005930 삼성전자 · "삼성전"→부분일치 OK · "엔비디아"/"nvda"→NVDA(NASDAQ, nationCode USA) · "애플"/"apple"→AAPL · "TIGER 미국"/"tiger"/"KODEX 200"→ETF OK | ✅ KR·US 1순위 |
| 〃 한글 브랜드 | "타이거"→타이거일렉(엉뚱) · "코덱스"→0건 · "S&P500"→0건 | ⚠️ 브랜드 별칭 표 필요(타이거→TIGER, 코덱스→KODEX …) |
| 〃 해외 기타 | TOKYO(JPN) 결과가 섞여 나옴 | nationCode KOR·USA 만 통과 |
| 〃 코인 | "비트코인"→나스닥 비트코인 인프라… (코인 아님) | ❌ 코인엔 못 씀 |
| 업비트 `v1/market/all` | 855개, `korean_name`(비트코인 등) 제공 | ✅ 코인 검색 소스 |
| 야후 검색 `v1/finance/search` | "nvidia"→NVDA + 선물·레버리지 ETF 섞임 | 보조(네이버 실패 시만) |
| `/api/stock-price` | `change`·`changePct`·`prevClose` 제공 | ✅ 오늘 손익 계산 가능 |
| 자산 화면 총자산 계산 | `assets/page.tsx` 클라이언트 인라인 · 가격 없으면 **매수가로 폴백** | ⚠️ 폴백이 실패를 숨김 — 홈에선 실패로 밝힌다 |
| `notices` | 1건(태그 필독, 2026-05-16) · `lounge_posts` 1건(05-11) | ⚠️ '선생님 한마디'에 그대로 쓰면 넉 달 전 글 |
| `/api/school-league` 프로덕션 | 200 · 8,170ms · **비로그인에도 실명·수익률·보유 상위 종목 반환** | 홈은 순위를 늦게 붙임. 보안 이슈는 별도 작업으로 분리(task_85e05685) |
| DDL 실행 경로 | `.env.local` 에 DB 접속 문자열 없음 · 관례 = SQL 파일 + SQL Editor 1회 실행 + 미생성 시 graceful(user_cash·re_watchlist) | 같은 관례 |
| PWA | manifest 없음 · public 에 svg 만 · sharp 미설치 | `app/manifest.ts` + `next/og` ImageResponse 로 PNG 아이콘 생성 |
| 디자인 시스템 | claude.ai "투자학교 디자인 시스템"(theme.ts 에서 생성, 토큰 이름 = TK/FS/RAD/SP 키) | ✅ 시안 기준 |

## 기각·보류한 안

- **35개 화면 전체 리디자인** — 기각. 몇 주 걸리고 근본 원인("화면이 너무 많다")이 남는다.
- **증권사 캡처 자동 등록** — 보류. 이중 입력을 정면으로 풀지만 개인 계좌 데이터라 유료 키 전용(`personal: true`)·오인식 확인 단계가 필요하다. 접속 기록 4주치로 매매 빈도를 본 뒤 결정(사용자도 학생 매매 빈도를 모른다고 함 — "자주 안 하는 거 같은 느낌 정도").
- **`last_sign_in_at` 을 접속 지표로** — 기각. 세션이 유지되는 동안 갱신되지 않는다(실측).
- **홈에서 리그 API 로 총자산까지** — 기각. 리그는 금액 비공개 설계라 총자산이 없고 8초 걸린다.

## 시안 반복 (Claude Design 캔버스 https://claude.ai/artifact/4THDHoK28RNSPeCjAcVvsa)

- 2판: 더리치·도미노·알파스퀘어 앱스토어 스크린샷 참고 → 머리글자 아이콘·추세선·자산 대 원금 차트·친구 포트폴리오 카드. 알파스퀘어의 매매 타이밍 화살표는 WHAT/WHEN 분리 원칙으로 기각.
- 3판: 제미나이 의견. 채택 = 코어/위성 배지·총액 요약 카드·수익률 계산 ? 도움말·간편/분석 모드 스위치·태블릿 세로 화면.
  **기각 = 시세 실패 시 가격 수동 입력**(출처 없는 숫자 — 홈은 '못 가져왔어요'로 밝힌다. 매매 체결가 수정은 원래 가능) ·
  **Tailwind 코드 프롬프트**(인라인 스타일 + TK 토큰 앱이라 제1원칙-b 위반).
- 4판: 챗지피티 의견. 채택 = 원금·불어난 돈을 총자산과 한 덩어리로 · 리그 한 줄 · 코어/위성 우리말 병기(기본/전략 투자) ·
  오늘의 투자 체크(사실만, 신규 자금 제안 — HOLD 원칙상 매도 권유 금지) · 종목 상세 화면 · 기록 빠른 선택 칩 · 매수가(입력) vs 지금 시세(참고) 분리 ·
  친구 '왜 달랐을까' 특징(섹터·국가는 기존 SSOT — 국가는 `etfLookThrough.countryWeights`, 상장 시장으로 세지 않는다) · 전체 메뉴 → 5분류 · 추세선 회색.
  **기각 = "잠시 지연되고 있습니다" 문구**(원인을 모르면서 '잠시'라고 단정 — '지금 시세를 못 가져왔어요'로 사실만) ·
  **분산점수 78점 같은 습관 점수**(정의·산정 근거 없는 점수 = 가짜 정밀. 순위 변화도 순위 이력 저장소가 없어 보류).
  **보류(사용자 결정) = 오늘의 미션**(누가 미션을 쓰나 — 선생님 직접 vs 보유 종목에서 자동) · 코어/위성 우리말 표기가 수업 용어와 맞는지.

- 5판(2026-09-26 사용자 결정): ① 선생님 한마디 → **오늘의 매매 브리핑**으로 대체 ② **'기본 투자·전략 투자' 같은 우리말 병기 금지** — 코어·위성만 쓴다
  ③ 배우기에 **오늘의 명언**(사용자 원래 구상 — 투자 거장 명언 한 줄) + 자동 미션 예시를 나란히 두고 결정 대기.
  ⚠️ 실측: `/briefing` 화면엔 **한 줄 요약 문장이 없다**(오늘 신호·실적·정리·담을 것 섹션 조합). 한 줄은 새로 만들어야 한다 —
  규칙 요약(섹션 건수) vs `hq-briefing` AI 문장(유료 키·학생별·12h 캐시·매번 문장이 흔들림). '정리할 것'(매도 후보)은 학생 홈 한 줄에 넣지 않는다(HOLD 원칙).
  ⚠️ 명언은 **출처(책·주주서한·연도)를 확인한 것만** — 투자 명언은 잘못된 인용이 흔하다(출처 없는 수치 금지의 인용판).

## 1단계 구현에서 정한 것 (2026-09-26, 브랜치 `feat/student-mode-phase1`)

작업마다 명세 검토 → 품질 검토 → 수정 → 재검토를 거쳤다. 검토가 뒤집은 것만 적는다.

| 주제 | 정한 것 | 왜 |
|---|---|---|
| 종목 검색 별칭 | 원문과 별칭 쿼리를 **동시에** 네이버에 보내 원문 결과를 앞에 | '에이스침대'가 'ACE침대'로 바뀌어 0건. 킨덱스·케이비스타·아리랑은 ACE·RISE·PLUS 로 이름이 바뀌었다(실측) |
| 검색 실패 | `failed` + `failedSources`(stocks·crypto) | 업비트 장애가 '그런 코인 없음'으로 보였다 |
| 코인 거래대금 | `ticker/all` 한 번(60초 캐시) | 종목별 조회는 상장폐지 1종이 섞이면 배치 전체 404 |
| 검증 스크립트 | 컴파일 전에 산출물 삭제 + tsc 실패 시 exit 1 | `noEmitOnError` 로 옛 .js 가 남아 **거짓 green** — 일부러 타입 오류를 넣어 EXIT=1 확인 |
| 히트맵 보합 칸 | `TK.line1`(#2a2d3a) | `TK.flat2` 는 카드 배경과 대비 1.06:1 로 안 보인다 |
| 보유 중복 | `dedupeHoldings`(id → 티커) — 자산 화면과 같은 규칙 | 안 하면 두 화면 총자산이 갈린다 |
| 지난 시세 | 시세 API 가 실패하고 캐시 값을 주면 **평가엔 쓰고 '지난 시세'로 표시**, '오늘'이라 쓰지 않는다 | 선생님 자산 화면이 그 값을 쓰므로 총액을 맞추되, 캐시 등락은 오늘 것이 아닐 수 있다 |
| 시세 전혀 없음 | 매수가로 평가 + "시세 못 가져옴 · 매수가로 계산" | ⚠️ 선생님 자산 화면은 이때 **0원(−100%)** 으로 계산한다 — 별도 작업으로 분리(이 브랜치 범위 밖). 그 경우에만 두 화면 총액이 다르다 |
| 종목 상세 차트 | 버튼 이름을 데이터 실제 기간으로(3개월·14개월·5년) | 시세 API 의 `1W`·`1M` 키가 이름과 다른 기간(주봉 60개·월봉 60개)이다 |
| 차트 '없음' vs '못 가져옴' | 쓸 데이터가 없으면 "못 가져왔어요" | 네이버·야후 차트 실패가 빈 배열로 온다. 평평한 선 제거는 코인만(API 가 가짜 평평선을 채운다) |
| 거래 기록 조회 | `user_id` 필터 필수 | 선생님 계정 RLS 는 학생 전체를 볼 수 있다 |
| 기록하기 쓰기 | 반영 행 1건 확인 · 다른 탭 수량 덮어쓰기 방지(`eq quantity`) · 매도는 **보유 반영 먼저, 거래 기록 나중** · 부분 실패면 잠금 | 0행 업데이트가 성공으로 보였다 / 재시도하면 매도가 두 번 기록됐다 |
| 코인 평단 | 100 이상 소수 2자리, 미만 유효숫자 8자리 | 0.015원이 0.02원(+33%)으로 저장될 뻔했다 |
| 소수 수량 | 모든 시장 허용 | 선생님 모달이 모두 허용(SSOT) |
| 보유 판정 | 티커만(대문자·공백 제거), 시장이 다르면 막는다 | DB 유니크 `(user_id, ticker)`·추가 모달과 같은 기준 |
| 숫자 표기 | `src/lib/studentFormat.ts` 한 곳 | 화면 4곳이 각자 계산해 0.015원/0.02원, `-`/`−` 가 갈렸다 |
| `kstDate` | 기록 화면에 복사 | `schoolIndex.ts` 가 yahoo-finance2(fs)를 끌고 와 브라우저 번들이 깨진다 |

**데이터 실측(읽기 전용, investments 73행)**: `KRW-`·`.KS` 형식 0 · 사용자+티커 중복 0 · 시장/통화 불일치 0 · KR 소수 수량 0 · KR 영숫자 코드(0131V0 등) 8행 → 티커 정규화는 지금 필요 없다.

**화면 실측(2026-09-26, 선생님 계정, 개발 서버)**
- 내 종목 평가금액 29,985,275원 · 불어난 돈 −2,090,080원(−6.5%) · 원금 32,075,355원 = 선생님 자산 화면 '총 ₩2,999만 · −₩209만(−6.5%) · 원금 ₩3,208만' (만원 단위 일치).
- 선생님 화면 **'총자산 ₩3,183만'은 달러 예수금 포함** → 학생 화면 이름을 '총자산'에서 **'내 종목 평가금액'**으로 바꿨다(같은 말로 다른 숫자 금지).
- 종목 상세 SK하이닉스: 평단 (1,745,000+900,000)/2 = 1,322,500 · 평가 3,726,000 · 손익 +1,081,000(+40.9%) · 비중 12.4% — 내 자산 목록 행과 일치.
- 🩹 **히트맵 작은 칸이 상자 밖으로 늘어났다** — `globals.css` 가 모바일에서 `a[href]` 에 `min-height: 44px` 를 건다. 절대배치 칸(높이 13~22px)이 44px 로 늘어 아래 카드를 덮었다. 칸 링크에 `minHeight: 0` 로 푼다. 칸 금액은 폭에 다 들어갈 때만.
- 🩹 **768px 에서 하단 탭과 왼쪽 메뉴가 같이 떴다** — 화면 배율 1.11 에서 폭이 768.4px 이라 `max-width:768px` 도 `min-width:769px` 도 안 맞았다. `not all and (min-width: 769px)` 로 정확한 여집합.
- 기록 쓰기: 평단가(1,322,500)로 매수 1 → 보유 3주·평단 그대로 → 같은 값에 매도 1 → 보유 2주·평단 1,322,500 복원, 매도 실현손익 0, `/history` 에 두 행 표시. **평단가로 한 이유**: 지금 시세로 사고 팔면 매도는 평단을 되돌리지 않아 선생님 평단이 바뀐다.
- 저장 후 내 자산으로 이동까지 개발 서버에서 10초 이상 — 서버 단계(스냅샷 0.3~1.9초·캐시 무효화 0.7초)는 빠르고 대부분 개발 컴파일. 프로덕션에서 다시 잰다.
- 선생님 화면 5종(/dashboard·/assets·/school-league·/signal-report·/swing) 기존 사이드바 유지, 학생 셸 누출 없음.

**배포**: main 병합(스쿨 리그 API 보안 수정 origin/main 먼저 병합) → `check:build` → 푸시 `0b9601d` → 프로덕션 `/api/stock-search` 가 새 응답(`failedSources`·에이스침대) 서빙 확인. 로그인 착지는 그대로라 학생에겐 아직 안 보인다.

## 미확인 가정

- 학생은 매매를 드물게 한다 → 홈은 '보기' 중심, 기록은 짧게. 4주 접속 기록으로 확인.
- 학생도 폰에서 주로 본다 → 폰 우선 설계, PC 는 같은 화면을 가운데 폭 제한.
