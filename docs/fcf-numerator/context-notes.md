# 💵 FCF 분자 신뢰성 규명 — 컨텍스트 노트 (2026-08-08)

## 왜 팠나
"에퀴노르 FCF수익률 31.8%"의 정체를 두 번 오판했다.
1차(8/07): "ADR 통화 불일치" → 실측 결과 EQNR은 재무·거래 모두 USD. **기각.**
2차(8/08): "호황 정점의 일시적 현금(사이클 정규화 필요)" → 연간 시계열이 6.0B인데
TTM이 29.4B라는 모순 발견. **기각.** 세 번째 가설이 이 문서다.

## 데이터 실측 판정표 — 결정적 증거
`financialData.freeCashflow`(앱이 쓰던 값) vs 분기 4개 합산(진짜 TTM) vs 최신 연간, 단위 $B

| 종목 | fd.freeCashflow | 분기4합(OCF−CapEx) | 연간 | fd.operatingCashflow | 분기4합 OCF |
|---|---|---|---|---|---|
| EQNR | **29.4** | 9.6 | 6.0 | 23.1 | 23.1 ✅ |
| MSFT | **16.5** | 67.0 | 67.0 | 182.9 | 182.9 ✅ |
| NVDA | **46.3** | 119.1 | 96.7 | 125.6 | 125.6 ✅ |
| AAPL | **107.7** | 136.7 | 98.8 | 146.7 | 146.7 ✅ |
| TSM | **739.1** | 1137.1 | 992.4 | 2634.7 | 2634.7 ✅ |
| COP | **7.4** | 5.9 | 7.2 | 21.9 | 18.0 |

**판정: `freeCashflow` 필드 하나만 신뢰 불가.**
- 🔴 결정적 반증 — EQNR은 **FCF(29.4) > OCF(23.1)**. CapEx가 음수여야 가능한 불가능한 값이다.
- ✅ 같은 응답의 `operatingCashflow` 는 분기 합산과 **완전 일치**(23.1·182.9·125.6·146.7·2634.7).
- ✅ FTS 의 freeCashFlow 도 `OCF + CapEx`(CapEx는 음수로 옴) 검산을 정확히 통과.
- 방향도 제각각 — MSFT·NVDA·AAPL·TSM은 **과소**, EQNR은 **과대**. 단일 배율 보정 불가.

## 채택
`getTrueFcf()` SSOT 신설 — 현금흐름표에서 직접 계산.
- 최근 4분기 합산(OCF−CapEx) = 진짜 TTM 우선 · 분기 결측이면 최신 연간 폴백 · 둘 다 없으면 **null(지표 보류)**
- 캐시 7일(분기 실적은 3개월에 한 번 바뀜 — 전수 스캔 호출 부담 제거)
- ⛔ `fd.freeCashflow` 폴백 금지 — 틀린 값을 채우느니 비워둔다

## 채택하지 않은 안과 이유
- **3년 평균 사이클 정규화(2차 가설)** — 증상(EQNR 31.8%)은 덮이지만 원인(분자 오류)을 가린다.
  분자를 고친 뒤에도 사이클 왜곡이 남는지는 **별도로 재검토**해야 할 문제.
- **fd.freeCashflow 를 배율 보정** — 과대·과소가 종목마다 뒤섞여 단일 보정 불가(위 표).
- **FMP 등 유료 전환** — 프로덕션과 일 쿼터를 공유해 스캔이 죽는다. 이미 기각한 안.

## 영향 (분자 교체 후 예상)
EQNR 31.8%→~10% · MSFT 0.4%→1.8% · NVDA 0.9%→2.2% · AAPL 상향 · TSM 상향.
쓰이는 곳: 통합추천 💎가치 축(FCF수익률)·🛟현금창출력 방어 가중·버핏 현금 체크·DCF 입력.

## 교훈
**"이상한 값"의 원인을 두 번 틀렸다.** 통화 → 사이클 → 필드 자체. 매번 그럴듯했고 매번
실측이 뒤집었다. 비율이 상식을 벗어나면 **분모(시총·통화)만 의심하지 말고 분자의 정의도
의심하라** — 그리고 같은 응답 안의 다른 필드(OCF)와 교차 검산하면 한 번에 갈린다.

## 사후검증 (2026-08-08 오후 — 교체 배포 직후)
① **커버리지**: 22종 실측 — 결측 0(ttm 20·annual 2, 둘 다 금융주라 지표 중립). 옛 분자엔
   있었는데 새 분자로 사라진 종목 0. ② **이상치 4종 원값 검산**: 롯데정밀화학 −20.9→+1.2%
   (부호 반전 — 연간 −0.04→+0.03조와 일관, 옛 값이 틀렸던 것)·HD현대 30.5%(연간 5.64·5.1조,
   실제 고FCF 지주)·IPARK 28.2%·LG전자 2.5%(CAPEX 4.07조가 OCF 잠식). **4종 모두 새 값 승.**

### ③ 검증이 잡은 신규 결함 — FTS 현금흐름의 통화는 financialCurrency가 아니다
통합추천 4위에 **두산밥캣 FCF수익률 18,310.8%**(우수 배지+방어 가중). 실측:
- `financialCurrency=USD` 인데 **FTS 분기 현금흐름은 KRW**(4분기 OCF 합 1.144조 — USD라면
  시총 6조 원 회사의 OCF가 1.1조 달러라는 불가능한 값). 환율 1,390을 또 곱해 ×1390 부풀림.
- 원인: **FTS(현금흐름표)는 상장지 공시 통화**(KRX→KRW), financialCurrency는 fd 모듈 통화.
  둘이 다를 수 있다(두산밥캣: 재무보고 USD·KRX 공시 KRW). TSM·SONY·EQNR(ADR)은 둘이 일치해
  우연히 통과했던 것.
- fd 필드(totalDebt 1.042B·totalRevenue 6.414B)는 **진짜 USD**(밥캣 실제 매출 $64억과 일치)
  → 출처별 분리: fd 값은 rate 곱, FTS 값은 **값 기반 통화 판별** 후 결정.
- 수정: `normalizeCashflow` 에 ①교차 검산(fd.operatingCashflow와 비율 ≈1=재무통화·≈환율=거래통화)
  ②타당성 검사(|FCF|/시총 ≤60%에 드는 쪽, 둘 다 밖이면 null 보류). KR 종목은 fd_ocf가 null이라
  ①이 못 돌고 ②가 잡는다(밥캣: 13.2% vs 18,310% → 전자 채택).
- 캐시: universe v14 · UNIFIED_RECO v59 · masters v12.
